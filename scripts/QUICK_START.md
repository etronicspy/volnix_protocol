# 🚀 Быстрый запуск локальной сети для разработки Volnix Protocol

⚠️ **ВНИМАНИЕ:** Этот скрипт предназначен **ТОЛЬКО для локальной разработки и тестирования**.

**Для production используйте Docker** - каждый валидатор в отдельном контейнере.

## 📋 Требования

### Обязательные зависимости

1. **Go 1.21+**
   ```bash
   go version
   ```
   Если не установлен: [Установка Go](https://golang.org/doc/install)

2. **Python 3**
   ```bash
   python3 --version
   ```
   Обычно предустановлен на macOS/Linux

3. **jq** (для работы с JSON)
   ```bash
   jq --version
   ```
   Установка:
   - **macOS**: `brew install jq`
   - **Linux (Ubuntu/Debian)**: `sudo apt-get install jq`
   - **Linux (Fedora/RHEL)**: `sudo dnf install jq`

4. **curl** (для проверки статуса)
   ```bash
   curl --version
   ```
   Обычно предустановлен

### Проверка всех зависимостей

```bash
# Запустите скрипт проверки
./scripts/start-local-dev-network.sh 2 2>&1 | head -20
# Если все зависимости установлены, вы увидите успешную инициализацию
```

## 🔨 Подготовка

### 1. Сборка бинарника (если еще не собран)

```bash
# Сборка volnixd-standalone
go build -o build/volnixd-standalone ./cmd/volnixd-standalone

# Или через Makefile
make build
```

### 2. Проверка бинарника

```bash
ls -lh build/volnixd-standalone
```

## 🚀 Запуск сети

### Базовый запуск (3 узла)

```bash
./scripts/start-local-dev-network.sh 3
```

### Запуск с очисткой данных

```bash
./scripts/start-local-dev-network.sh 3 --clean
```

### Запуск с другим количеством узлов

```bash
# Минимум 2 узла
./scripts/start-local-dev-network.sh 2 --clean

# 4 узла
./scripts/start-local-dev-network.sh 4 --clean
```

## ✅ Проверка работы сети

### Проверка статуса узлов

```bash
# Проверка всех узлов
for port in 26657 26667 26677; do
  echo "Порт $port:"
  curl -s http://localhost:$port/status | jq -r '.result.sync_info.latest_block_height // "недоступен"'
done
```

### Проверка одного узла

```bash
# Статус node0
curl http://localhost:26657/status | jq

# Информация о блоке
curl http://localhost:26657/block | jq
```

### Просмотр логов

```bash
# Логи конкретного узла
tail -f logs/node0.log

# Логи всех узлов
tail -f logs/*.log
```

## 🛑 Остановка сети

### Автоматическая остановка

Нажмите `Ctrl+C` в терминале, где запущен скрипт.

### Ручная остановка

```bash
# Остановка всех процессов
pkill -f "volnixd-standalone"

# Или через PID файл
if [ -f .network_pids ]; then
  while read pid; do
    kill "$pid" 2>/dev/null || true
  done < .network_pids
  rm -f .network_pids
fi
```

## 📊 Структура после запуска

```
testnet/
├── node0/
│   └── .volnix/
│       ├── config/
│       │   ├── config.toml      # Конфигурация узла
│       │   ├── genesis.json     # Genesis файл
│       │   └── priv_validator_key.json
│       └── data/                # База данных
├── node1/
│   └── .volnix/...
├── node2/
│   └── .volnix/...
└── genesis.json                 # Общий genesis файл

logs/
├── node0.log
├── node1.log
└── node2.log
```

## 🔧 Устранение неполадок

### Проблема: "volnixd-standalone не найден"

**Решение:**
```bash
go build -o build/volnixd-standalone ./cmd/volnixd-standalone
```

### Проблема: "jq не установлен"

**Решение:**
```bash
# macOS
brew install jq

# Linux
sudo apt-get install jq  # Ubuntu/Debian
sudo dnf install jq      # Fedora/RHEL
```

### Проблема: "Порт уже занят"

**Решение:**
```bash
# Остановите все процессы
pkill -f "volnixd-standalone"

# Или проверьте, какой процесс использует порт
lsof -i :26657
```

### Проблема: "Узел не инициализирован"

**Решение:**
```bash
# Запустите с флагом --clean
./scripts/start-local-dev-network.sh 3 --clean
```

### Проблема: Узлы не синхронизируются

**Решение:**
1. Проверьте, что все узлы используют один genesis файл:
   ```bash
   diff testnet/node0/.volnix/config/genesis.json testnet/node1/.volnix/config/genesis.json
   ```

2. Проверьте peer connections:
   ```bash
   grep persistent_peers testnet/node0/.volnix/config/config.toml
   ```

3. Перезапустите с очисткой:
   ```bash
   rm -rf testnet .volnix .network_pids
   ./scripts/start-local-dev-network.sh 3 --clean
   ```

## 📝 Примеры использования

### Мониторинг блоков

```bash
# Следить за созданием новых блоков
watch -n 1 'curl -s http://localhost:26657/status | jq -r ".result.sync_info.latest_block_height"'
```

### Проверка валидаторов

```bash
# Список валидаторов
curl http://localhost:26657/validators | jq
```

### Проверка консенсуса

```bash
# Информация о консенсусе
curl http://localhost:26657/consensus_state | jq
```

## 🎯 Рекомендации

1. **Минимальное количество узлов**: 2 (для тестирования), рекомендуется 3-4
2. **Очистка данных**: Используйте `--clean` при первом запуске или при проблемах
3. **Мониторинг**: Следите за логами для выявления проблем
4. **Порты**: По умолчанию используются порты 26656-26677 (можно изменить в скрипте)

## 📚 Дополнительная информация

- Подробная документация: `scripts/README-minimal-network.md`
- Основной README: `README.md`
- Структура проекта: `docs/`

