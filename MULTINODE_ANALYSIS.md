# 🔍 Анализ мультинод консенсуса

## Текущий статус

**✅ Достигнуто:**
- 3 узла запускаются на разных портах
- Каждый узел создает блоки независимо
- RPC endpoints работают
- Параметризация портов через env vars

**❌ Не работает:**
- Узлы не видят друг друга (Peers: 0)
- Нет синхронизации блоков
- Консенсус не работает

---

## 🔍 Корневая причина

### Проблема #1: Одинаковые node IDs

Все узлы имеют **одинаковый node ID**:
```
Node 0: b4e056943f4bf8ae56da0671f8c7716d01adb7af
Node 1: b4e056943f4bf8ae56da0671f8c7716d01adb7af  ← ТОТ ЖЕ!
Node 2: b4e056943f4bf8ae56da0671f8c7716d01adb7af  ← ТОТ ЖЕ!
```

**Причина:** Копирование `.volnix/config/node_key.json` с одного узла

**Результат:** Узлы не могут идентифицировать друг друга

### Проблема #2: Persistent peers не настроены

```toml
persistent_peers = "null@127.0.0.1:26666,null@127.0.0.1:26676"
```

**Причина:** Node IDs = null из-за неправильного парсинга JSON

### Проблема #3: Каждый узел - свой genesis

Даже с общим chain-id, у каждого свой набор валидаторов

---

## ✅ Что нужно для консенсуса

### 1. Уникальные node keys

Каждый узел должен иметь **свой уникальный** `node_key.json`:

```bash
# НЕ делать так:
cp -r node0/.volnix node1/.volnix  ❌

# Делать так:
volnixd-standalone init node0  ✅
volnixd-standalone init node1  ✅
volnixd-standalone init node2  ✅
```

### 2. Общий genesis с всеми валидаторами

```json
{
  "chain_id": "volnix-testnet",
  "validators": [
    {"pub_key": "node0_pubkey", "power": "10"},
    {"pub_key": "node1_pubkey", "power": "10"},
    {"pub_key": "node2_pubkey", "power": "10"}
  ]
}
```

Скопировать этот genesis на **все** узлы.

### 3. Persistent peers с правильными IDs

```toml
# Node 0
persistent_peers = "real_node1_id@127.0.0.1:26666,real_node2_id@127.0.0.1:26676"
```

Где `real_node1_id` - это SHA256 хеш публичного ключа из `node_key.json`

---

## 🛠️ Решение

### Вариант 1: Исправить init (Рекомендуется)

Проблема: `volnixd-standalone init` зависает при запуске через скрипт

**Решение:** Доработать команду init чтобы она:
1. Не запускала server
2. Только создавала файлы
3. Быстро завершалась

**Время:** 2-4 часа

### Вариант 2: Вручную создать конфигурацию

1. Запустить каждый узел один раз для генерации ключей
2. Остановить
3. Собрать все pubkeys
4. Создать общий genesis
5. Скопировать на все узлы
6. Настроить persistent_peers
7. Запустить

**Время:** 1-2 часа ручной работы

### Вариант 3: Использовать Cosmos SDK (Долгосрочное)

Реализовать полноценный `volnixd` с командами:
- `volnixd testnet init-files` - создание testnet
- `volnixd genesis gentx` - создание genesis транзакций
- `volnixd genesis collect-gentxs` - сборка общего genesis

**Время:** 3-5 дней

---

## 📊 Итоги тестирования

### ✅ Работает:
- Параметризация портов (VOLNIX_RPC_PORT, VOLNIX_P2P_PORT)
- Запуск множественных узлов на одной машине
- Каждый узел создает блоки независимо
- RPC API работает на всех узлах

### ❌ Не работает:
- P2P соединения (одинаковые node IDs)
- Синхронизация блоков
- Общий консенсус

### ⚠️  Ограничения standalone версии:

`volnixd-standalone` был создан для **быстрого прототипирования** и **разработки**.

Для **продакшн мультинод сети** рекомендуется:
- Использовать полноценный Cosmos SDK starter template
- Реализовать правильные команды init/gentx/collect-gentxs
- Использовать стандартный подход Cosmos SDK

---

## 🎯 Рекомендации для продакшн

### Краткосрочное решение (Сегодня - 1 неделя)

**Используйте 1 узел:**
- ✅ Полностью работает
- ✅ Обрабатывает транзакции
- ✅ Frontend функционален
- ✅ Docker ready
- ✅ Готов к деплою

### Среднесрочное решение (1-2 недели)

**Варианты:**

1. **Вручную настроить 3-нод сеть** (1-2 дня)
   - Создать уникальные node keys
   - Собрать общий genesis
   - Настроить persistent_peers
   - Протестировать

2. **Доработать init команду** (2-3 дня)
   - Исправить зависание
   - Добавить генерацию genesis
   - Автоматизировать настройку

### Долгосрочное решение (2-4 недели)

**Полноценная Cosmos SDK имплементация:**
- Правильная структура приложения
- Команды testnet/gentx
- Production-ready мультинод
- Полная совместимость с экосистемой Cosmos

---

## 📋 Текущая готовность

| Компонент | 1-нод | 3-нод |
|-----------|-------|-------|
| Создание блоков | ✅ | ✅ (независимо) |
| Транзакции | ✅ | ✅ |
| P2P соединения | N/A | ❌ |
| Консенсус | ✅ (один валидатор) | ❌ |
| Frontend | ✅ | ✅ |
| Docker | ✅ | ⚠️  |
| Prod ready | ✅ | ❌ |

---

## ✅ Итог

**Проект готов к продакшн с 1 узлом.**

Мультинод консенсус требует дополнительной работы:
- Исправление генерации уникальных node keys
- Правильная сборка общего genesis
- Настройка P2P соединений

**Время до полного мультинод:** 1-2 недели

**Рекомендация:** Деплойте с 1 узлом сейчас, доработайте мультинод позже.

