# 🧪 Testnet Status

## Текущий статус

**3 узла запущены локально** на портах 26657, 26757, 26857

**Проблема:** Узлы работают независимо, не синхронизируются
- Каждый узел имеет свой genesis файл
- Узлы не являются валидаторами друг для друга
- "This node is not a validator" в логах
- "Blockpool has no peers" - нет P2P соединений

## Почему не работает консенсус

### Требуется для мультинод консенсуса:

1. **Общий genesis файл** с всеми валидаторами:
```json
{
  "validators": [
    {"pub_key": "node0_pubkey", "power": "100"},
    {"pub_key": "node1_pubkey", "power": "100"},
    {"pub_key": "node2_pubkey", "power": "100"}
  ]
}
```

2. **P2P соединения** в config.toml:
```toml
persistent_peers = "node1_id@127.0.0.1:26756,node2_id@127.0.0.1:26856"
```

3. **Одинаковый chain-id** на всех узлах

## Что работает

✅ **Standalone узел (1 узел):**
- Создает блоки каждые 5 секунд
- Обрабатывает транзакции
- Query endpoints работают
- События эмитятся
- Frontend полностью функционален

**Запуск:**
```bash
./build/volnixd-standalone start
# или
bash scripts/start-minimal-network.sh
```

## Решение для мультинод

### Вариант 1: Использовать существующий скрипт

```bash
bash scripts/start-minimal-network.sh
```

Этот скрипт создает рабочий testnet с 3 узлами и общим genesis.

### Вариант 2: Docker

```bash
docker-compose up -d
```

Docker Compose настроит общий genesis и P2P соединения.

### Вариант 3: Ручная настройка

1. Инициализировать узлы
2. Собрать pubkeys всех валидаторов
3. Создать общий genesis с всеми валидаторами
4. Скопировать genesis на все узлы
5. Настроить persistent_peers
6. Запустить узлы

## Рекомендация

Для демонстрации работающего testnet:

```bash
# Запустить полнофункциональный testnet
bash scripts/start-minimal-network.sh

# Проверить
curl http://localhost:26657/status

# Frontend
cd frontend/wallet-ui && npm start
```

Этот testnet работает из коробки с правильной конфигурацией консенсуса.

