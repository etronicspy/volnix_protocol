# Dependency Management

## Go Modules
- Use Go modules, pin dependency versions in `go.mod`, use `go.sum`
- Pin Cosmos SDK: `v0.53.x`, CometBFT: `v0.38.x`
- Use `replace` for local development, remove before release

## Security & Updates
- Use `go list -m -u` for updates, `govulncheck` for vulnerability scanning
- Review security advisories, update vulnerable dependencies promptly
- Minimize dependency count

## Frontend Dependencies
- Use package-lock.json or yarn.lock, pin dependency versions
- Use `npm audit` or `yarn audit` for security
