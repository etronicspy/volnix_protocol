# 🚀 Развертывание Volnix Protocol в продакшн

## 📊 Текущее состояние проекта

### ✅ Что готово

1. **Standalone узел**
   - Создает блоки
   - Обрабатывает транзакции (MsgSend)
   - Запросы балансов работают
   - События эмитятся
   - Тесты проходят

2. **Модули реализованы**
   - `x/ident` - Identity verification with ZKP
   - `x/lizenz` - LZN license management
   - `x/anteil` - ANT market and trading
   - `x/consensus` - PoVB consensus algorithm
   - `x/governance` - On-chain governance

3. **Frontend**
   - Wallet UI работает
   - Отправка транзакций
   - История транзакций
   - Отображение балансов

### ⚠️  Что требует доработки для мультинод сети

1. **Порты**
   - В standalone версии порты жестко заданы (26657, 26656)
   - Требуется: параметризация портов через переменные окружения

2. **Genesis файл**
   - Требуется: настройка genesis для мультинод сети
   - Добавление множественных валидаторов
   - Настройка начальных балансов

3. **P2P конфигурация**
   - Настройка persistent_peers
   - Настройка seed узлов

## 🔧 Рекомендации для продакшн развертывания

### Вариант 1: Standalone для разработки/тестирования

**Текущая реализация готова для:**
- Разработки и тестирования
- Демонстрации функциональности
- Локальной сети с одним узлом

**Использование:**
```bash
# Сборка
make build-standalone

# Запуск
./build/volnixd-standalone start

# Frontend
cd frontend/wallet-ui
npm start
```

**Endpoints:**
- RPC: http://localhost:26657
- Frontend: http://localhost:3000

### Вариант 2: Доработка для продакшн (Рекомендуется)

#### 2.1. Параметризация портов

Добавить поддержку переменных окружения в `cmd/volnixd-standalone/main.go`:

```go
// Получение портов из env или использование дефолтных
rpcPort := os.Getenv("VOLNIX_RPC_PORT")
if rpcPort == "" {
    rpcPort = "26657"
}

p2pPort := os.Getenv("VOLNIX_P2P_PORT")
if p2pPort == "" {
    p2pPort = "26656"
}

config.RPC.ListenAddress = fmt.Sprintf("tcp://0.0.0.0:%s", rpcPort)
config.P2P.ListenAddress = fmt.Sprintf("tcp://0.0.0.0:%s", p2pPort)
```

#### 2.2. Мультинод genesis

Создать `scripts/create-production-genesis.sh`:
- Генерация ключей валидаторов
- Создание genesis с множественными валидаторами
- Настройка начальных балансов

#### 2.3. Запуск мультинод сети

```bash
# Node 0
VOLNIX_RPC_PORT=26657 VOLNIX_P2P_PORT=26656 ./build/volnixd-standalone start

# Node 1
VOLNIX_RPC_PORT=26757 VOLNIX_P2P_PORT=26756 ./build/volnixd-standalone start

# Node 2
VOLNIX_RPC_PORT=26857 VOLNIX_P2P_PORT=26856 ./build/volnixd-standalone start
```

### Вариант 3: Использование Cosmos SDK scaffolding

Для полноценной продакшн сети рекомендуется:

1. Использовать `ignite CLI` для создания полноценной структуры
2. Интегрировать существующие модули
3. Настроить полный стек CometBFT с правильной инициализацией

## 📋 Пошаговый план подготовки к продакшн

### Этап 1: Минимальная продакшн готовность (1-2 дня)

- [ ] Добавить параметризацию портов через env vars
- [ ] Создать скрипт генерации genesis для мультинод сети
- [ ] Протестировать 3-нод сеть локально
- [ ] Проверить синхронизацию блоков между узлами

### Этап 2: Базовая продакшн готовность (3-5 дней)

- [ ] Настроить логирование и мониторинг
- [ ] Добавить health checks
- [ ] Создать Docker образы
- [ ] Подготовить Docker Compose для развертывания
- [ ] Настроить CI/CD pipeline

### Этап 3: Полная продакшн готовность (1-2 недели)

- [ ] Интеграция всех модулей (ident, lizenz, anteil, consensus)
- [ ] Полноценная реализация PoVB консенсуса
- [ ] Stress testing и load testing
- [ ] Security audit
- [ ] Документация для операторов узлов
- [ ] Создание testnet
- [ ] Подготовка к mainnet launch

## 🐳 Docker развертывание

### Dockerfile

```dockerfile
FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY . .
RUN make build-standalone

FROM alpine:latest
RUN apk add --no-cache ca-certificates
COPY --from=builder /app/build/volnixd-standalone /usr/local/bin/
ENTRYPOINT ["volnixd-standalone"]
CMD ["start"]
```

### Docker Compose

```yaml
version: '3.8'

services:
  validator-0:
    build: .
    environment:
      - VOLNIX_RPC_PORT=26657
      - VOLNIX_P2P_PORT=26656
    ports:
      - "26657:26657"
      - "26656:26656"
    volumes:
      - ./mainnet/validator-0/.volnix:/root/.volnix

  validator-1:
    build: .
    environment:
      - VOLNIX_RPC_PORT=26757
      - VOLNIX_P2P_PORT=26756
    ports:
      - "26757:26757"
      - "26756:26756"
    volumes:
      - ./mainnet/validator-1/.volnix:/root/.volnix
```

## 🔐 Безопасность

### Ключи валидаторов

- Использовать hardware security modules (HSM) для приватных ключей
- Резервное копирование ключей
- Мультиподписные схемы для критичных операций

### Сетевая безопасность

- Firewall конфигурация
- VPN для соединений между узлами
- DDoS защита для публичных RPC endpoints

## 📈 Мониторинг

### Prometheus метрики

- Block height
- Transaction throughput
- Consensus round time
- Peer connections
- Memory/CPU usage

### Alerting

- Узел отстает от сети
- Высокое использование ресурсов
- Потеря соединений с пирами
- Ошибки консенсуса

## 🎯 Текущие рекомендации

**Для немедленного использования:**
- Используйте standalone версию для разработки и тестирования
- Один узел достаточен для демонстрации функциональности

**Для продакшн развертывания:**
- Выделите 1-2 недели на доработку
- Начните с параметризации портов
- Протестируйте на testnet перед mainnet

**Альтернатива:**
- Рассмотрите использование Cosmos SDK starter template
- Интегрируйте существующие модули в правильную структуру
- Используйте production-ready компоненты из экосистемы Cosmos

## 📞 Следующие шаги

1. Определите требования к продакшн сети:
   - Количество валидаторов
   - Требования к производительности
   - Географическое распределение

2. Выберите вариант реализации:
   - Быстрая доработка standalone
   - Полная реализация на Cosmos SDK

3. Подготовьте инфраструктуру:
   - Сервера/облако
   - Мониторинг
   - Backup системы

4. Создайте testnet для тестирования

5. Запустите mainnet после полного тестирования

