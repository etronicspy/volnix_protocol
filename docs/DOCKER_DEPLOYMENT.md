# 🐳 Docker Deployment Guide для Validators

> **Этот документ предназначен для пользователей, которые хотят запустить свой узел/валидатор в сети Volnix Protocol**

## 📋 Обзор

Этот гайд описывает, как развернуть **один валидатор** в Docker контейнере. Каждый валидатор запускается в **своем собственном контейнере**, и сеть формируется из множества таких независимых контейнеров.

## 🎯 Что вы получите

После развертывания у вас будет:
- ✅ Полноценный узел блокчейна Volnix Protocol
- ✅ Автоматическая инициализация при первом запуске
- ✅ Персистентное хранение данных
- ✅ Health checks и мониторинг
- ✅ Production-ready конфигурация

## 📦 Требования

- Docker 20.10+
- Docker Compose 2.0+ (опционально, только для тестирования)
- Минимум 4GB RAM
- 20GB+ свободного места на диске
- Доступ к сети (для синхронизации с другими валидаторами)

## 🚀 Быстрый старт

### 1. Сборка образа

```bash
# Клонируйте репозиторий
git clone https://github.com/volnix-protocol/volnix-protocol.git
cd volnix-protocol

# Соберите Docker образ
docker build -t volnix-validator:latest \
  --build-arg VERSION=$(git describe --tags --always) \
  --build-arg COMMIT=$(git rev-parse --short HEAD) \
  --build-arg BUILD_TIME=$(date -u +"%Y-%m-%dT%H:%M:%SZ") \
  .
```

### 2. Запуск валидатора

**Вариант 1: Используя Docker Compose (рекомендуется)**

```bash
# Создайте .env файл (опционально) для настройки
cat > .env << EOF
MONIKER=my-validator
CHAIN_ID=volnix-mainnet
RPC_PORT=26657
P2P_PORT=26656
CPU_LIMIT=2.0
MEMORY_LIMIT=4G
EOF

# Запустите валидатор
docker-compose up -d

# Проверьте статус
docker-compose ps
docker-compose logs -f
```

**Вариант 2: Используя docker run**

```bash
# Создайте директории для данных
mkdir -p ~/volnix-validator/{data,config}

# Запустите контейнер
docker run -d \
  --name volnix-validator \
  --restart unless-stopped \
  -p 26656:26656 \
  -p 26657:26657 \
  -v volnix-validator-data:/home/volnix/.volnix/data \
  -v volnix-validator-config:/home/volnix/.volnix/config:ro \
  -e MONIKER=my-validator \
  -e CHAIN_ID=volnix-mainnet \
  volnix-validator:latest
```

### 3. Проверка статуса

**С Docker Compose:**
```bash
# Проверьте логи
docker-compose logs -f

# Проверьте статус через RPC
curl http://localhost:26657/status | jq

# Используйте встроенную утилиту
docker-compose exec validator node-info.sh
```

**С docker run:**
```bash
# Проверьте логи
docker logs -f volnix-validator

# Проверьте статус через RPC
curl http://localhost:26657/status | jq

# Используйте встроенную утилиту
docker exec volnix-validator node-info.sh
```

## ⚙️ Конфигурация

### Environment Variables

**При использовании docker-compose.yml** (можно задать через `.env` файл или переменные окружения):

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `MONIKER` | Имя валидатора | `validator` |
| `CHAIN_ID` | ID блокчейна | `volnix-mainnet` |
| `RPC_PORT` | Порт RPC на хосте | `26657` |
| `P2P_PORT` | Порт P2P на хосте | `26656` |
| `CPU_LIMIT` | Лимит CPU | `2.0` |
| `MEMORY_LIMIT` | Лимит памяти | `4G` |
| `CPU_RESERVATION` | Резервация CPU | `1.0` |
| `MEMORY_RESERVATION` | Резервация памяти | `2G` |

**Внутри контейнера** (устанавливаются автоматически):

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `VOLNIX_HOME` | Домашняя директория | `/home/volnix/.volnix` |
| `VOLNIX_RPC_PORT` | Порт RPC сервера | `26657` |
| `VOLNIX_P2P_PORT` | Порт P2P сервера | `26656` |

### Volumes

**Данные (data volume):**
- Хранит блокчейн данные (блоки, состояние, БД)
- Должен быть персистентным
- Рекомендуется регулярное резервное копирование

**Конфигурация (config volume):**
- Хранит конфигурационные файлы
- Монтируется read-only для безопасности
- Содержит: `genesis.json`, `config.toml`, ключи валидатора

### Порты

- **26656 (P2P)**: Межузловое взаимодействие (должен быть доступен извне)
- **26657 (RPC)**: HTTP API (можно ограничить доступ)
- **1317 (REST API)**: RESTful API (опционально)
- **9090 (gRPC)**: gRPC API (опционально)

## 🔗 Подключение к сети

### Настройка persistent_peers

После первого запуска настройте подключение к другим валидаторам:

```bash
# Войдите в контейнер
docker exec -it volnix-validator sh

# Отредактируйте config.toml (нужно будет перемонтировать volume как read-write)
# Или используйте docker volume для редактирования

# Формат persistent_peers:
# node_id@ip:port,node_id2@ip2:port2
```

Пример `config.toml`:
```toml
[p2p]
persistent_peers = "abc123@validator1.example.com:26656,def456@validator2.example.com:26656"
seeds = "seed1.volnix.network:26656,seed2.volnix.network:26656"
```

### Получение Node ID

```bash
# Получите Node ID вашего валидатора
docker exec volnix-validator cat /home/volnix/.volnix/config/node_key.json | jq -r '.id'

# Или через RPC
curl -s http://localhost:26657/status | jq -r '.result.node_info.id'
```

## 📊 Мониторинг

### Health Checks

Docker автоматически проверяет здоровье контейнера:

```bash
# Проверка статуса health check
docker inspect volnix-validator | jq '.[0].State.Health'
```

### Логи

```bash
# Просмотр логов
docker logs -f volnix-validator

# Последние 100 строк
docker logs --tail=100 volnix-validator

# Логи с временными метками
docker logs -f -t volnix-validator
```

### Метрики

```bash
# Статус узла
curl http://localhost:26657/status | jq

# Информация о сети
curl http://localhost:26657/net_info | jq

# Высота блока
curl -s http://localhost:26657/status | jq -r '.result.sync_info.latest_block_height'
```

## 💾 Управление данными

### Backup

```bash
# Создание backup данных
docker run --rm \
  -v volnix-validator-data:/data:ro \
  -v $(pwd):/backup \
  alpine tar czf /backup/validator-backup-$(date +%Y%m%d).tar.gz -C /data .
```

### Восстановление

```bash
# Остановите контейнер
docker stop volnix-validator

# Восстановите данные
docker run --rm \
  -v volnix-validator-data:/data \
  -v $(pwd):/backup \
  alpine sh -c "rm -rf /data/* && tar xzf /backup/validator-backup-YYYYMMDD.tar.gz -C /data"

# Запустите контейнер
docker start volnix-validator
```

## 🔄 Обновление

### Обновление образа

**С Docker Compose:**
```bash
# 1. Остановите контейнер
docker-compose down

# 2. Соберите новый образ
docker-compose build

# 3. Запустите обновленный контейнер (данные сохранятся в volumes)
docker-compose up -d
```

**С docker run:**
```bash
# 1. Остановите контейнер
docker stop volnix-validator

# 2. Соберите новый образ
docker build -t volnix-validator:latest .

# 3. Удалите старый контейнер (данные сохранятся в volume)
docker rm volnix-validator

# 4. Запустите новый контейнер с теми же volumes
docker run -d \
  --name volnix-validator \
  --restart unless-stopped \
  -p 26656:26656 \
  -p 26657:26657 \
  -v volnix-validator-data:/home/volnix/.volnix/data \
  -v volnix-validator-config:/home/volnix/.volnix/config:ro \
  -e MONIKER=my-validator \
  -e CHAIN_ID=volnix-mainnet \
  volnix-validator:latest
```

## 🔐 Безопасность

### Рекомендации

1. **Firewall**: Откройте только необходимые порты (26656 для P2P)
2. **RPC доступ**: Ограничьте доступ к порту 26657 (только для локального использования или через reverse proxy)
3. **Ключи**: Никогда не коммитьте приватные ключи в репозиторий
4. **Volumes**: Используйте Docker volumes, не bind mounts для конфиденциальных данных
5. **Обновления**: Регулярно обновляйте образ для получения исправлений безопасности

### Пример настройки firewall (UFW)

```bash
# Разрешить P2P порт
sudo ufw allow 26656/tcp

# Разрешить RPC только с локального хоста (опционально)
sudo ufw allow from 127.0.0.1 to any port 26657
```

## 🐛 Troubleshooting

### Контейнер не запускается

```bash
# Проверьте логи
docker logs volnix-validator

# Проверьте ресурсы
docker stats volnix-validator

# Проверьте конфигурацию
docker exec volnix-validator cat /home/volnix/.volnix/config/config.toml
```

### Узел не синхронизируется

1. Проверьте persistent_peers в config.toml
2. Проверьте доступность порта 26656 извне
3. Проверьте логи на ошибки подключения
4. Убедитесь, что genesis.json совпадает с сетью

### Проблемы с данными

```bash
# Проверьте размер volume
docker system df -v

# Проверьте права доступа
docker exec volnix-validator ls -la /home/volnix/.volnix
```

## 📚 Дополнительные ресурсы

- [Docker Architecture](DOCKER_ARCHITECTURE.md) - Детальная архитектура Docker образа
- [Network Infrastructure](../docs/infrastructure/07-network-infrastructure-guide.md) - Сетевая инфраструктура
- [State Sync](state-sync.md) - Быстрая синхронизация состояния

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker logs -f volnix-validator`
2. Проверьте статус: `curl http://localhost:26657/status`
3. Создайте issue в репозитории с логами и конфигурацией

