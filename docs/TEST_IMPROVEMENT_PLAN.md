# План улучшения тестового покрытия

**Дата создания:** 9 ноября 2025  
**Текущее покрытие:** 9.1%  
**Целевое покрытие:** 80%+

---

## 🎯 Приоритеты улучшения

### Приоритет 1: Критические исправления (1-2 недели)
Исправить падающие тесты и критические проблемы инфраструктуры.

### Приоритет 2: Улучшение покрытия (2-4 недели)
Добавить тесты для непокрытых модулей и функций.

### Приоритет 3: Оптимизация и расширение (1-2 месяца)
Улучшить существующие тесты и добавить edge cases.

---

## 🔧 Приоритет 1: Исправление критических проблем

### 1.1 Исправить падающие msg_server тесты (7 тестов)

#### Проблема:
- 3 теста в `x/ident/keeper/msg_server_test.go`
- 4 теста в `x/anteil/keeper/msg_server_test.go`

#### Действия:
```bash
# 1. Запустить падающие тесты с подробным выводом
go test -v ./x/ident/keeper/... -run TestMsgServer
go test -v ./x/anteil/keeper/... -run TestMsgServer

# 2. Проанализировать ошибки
# 3. Исправить проблемы в msg_server или тестах
```

#### Ожидаемый результат:
- ✅ Все msg_server тесты проходят
- ✅ Покрытие msg_server увеличивается до 80%+

#### Шаги:
1. **Анализ ошибок:**
   - Запустить тесты с `-v` флагом
   - Сохранить вывод в файл для анализа
   - Определить корневую причину ошибок

2. **Исправление:**
   - Проверить инициализацию keeper'ов в тестах
   - Убедиться, что все зависимости правильно настроены
   - Проверить валидацию входных данных
   - Исправить проблемы с контекстом

3. **Верификация:**
   - Запустить все тесты модуля
   - Проверить, что не сломались другие тесты
   - Обновить документацию

---

### 1.2 Исправить проблему "store does not exist" (13 тестов)

#### Проблема:
- Паника "store does not exist" в integration/security/end-to-end тестах
- Проблемы с инициализацией store в тестовом окружении

#### Действия:
```go
// Пример правильной инициализации store
func (suite *TestSuite) SetupTest() {
    // Создать все необходимые store keys
    suite.identStoreKey = storetypes.NewKVStoreKey("ident")
    suite.lizenzStoreKey = storetypes.NewKVStoreKey("lizenz")
    // ...
    
    // Создать CommitMultiStore и смонтировать все stores
    cms := store.NewCommitMultiStore(db, log.NewNopLogger(), metrics.NewNoOpMetrics())
    cms.MountStoreWithDB(suite.identStoreKey, storetypes.StoreTypeIAVL, db)
    cms.MountStoreWithDB(suite.lizenzStoreKey, storetypes.StoreTypeIAVL, db)
    // ...
    
    // Загрузить версию
    err := cms.LoadLatestVersion()
    require.NoError(suite.T(), err)
    
    // Создать контекст
    suite.ctx = sdk.NewContext(cms, cmtproto.Header{}, false, log.NewNopLogger())
}
```

#### Ожидаемый результат:
- ✅ Все integration тесты проходят
- ✅ Все security тесты проходят
- ✅ Все end-to-end тесты проходят

#### Шаги:
1. **Создать helper функцию для инициализации:**
   ```go
   // tests/test_helpers.go
   func NewTestContext(storeKeys ...storetypes.StoreKey) sdk.Context {
       // Единая логика инициализации для всех тестов
   }
   ```

2. **Обновить все тестовые suite:**
   - Использовать helper функцию
   - Убедиться, что все store keys правильно смонтированы
   - Проверить порядок инициализации

3. **Добавить проверки:**
   - Проверять наличие store перед использованием
   - Добавить понятные сообщения об ошибках

---

### 1.3 Исправить проблему "Account limit exceeded"

#### Проблема:
- Ошибка "Account limit exceeded" в end-to-end тестах
- Проблема конфигурации параметров

#### Действия:
```go
// В SetupTest увеличить лимиты для тестирования
func (suite *EndToEndTestSuite) SetupTest() {
    // ...
    identParams := identtypes.DefaultParams()
    identParams.MaxIdentitiesPerAddress = 1000 // Увеличить для тестов
    suite.identKeeper.SetParams(suite.ctx, identParams)
    // ...
}
```

#### Ожидаемый результат:
- ✅ Все end-to-end тесты проходят
- ✅ Тесты не ограничены конфигурацией

---

### 1.4 Исправить проблемы с контекстом в benchmark тестах

#### Проблема:
- Некоторые benchmark тесты пропущены из-за проблем с контекстом
- Проблемы с multi-store контекстом

#### Действия:
```go
// Исправить создание контекста для каждого keeper
func BenchmarkCreateOrder(b *testing.B) {
    // Создать отдельный контекст для каждого keeper
    ctx := createContextForKeeper(storeKey, paramStore)
    // ...
}
```

#### Ожидаемый результат:
- ✅ Все benchmark тесты работают
- ✅ Можно измерять производительность

---

## 📈 Приоритет 2: Улучшение покрытия

### 2.1 Добавить тесты для app/ модуля

#### Текущее состояние:
- ❌ Нет тестов для `app/` модуля
- Критический модуль для инициализации приложения

#### План тестирования:
```go
// app/app_test.go
func TestNewApp(t *testing.T) {
    // Тест создания нового приложения
}

func TestAppInitGenesis(t *testing.T) {
    // Тест инициализации genesis
}

func TestAppExportGenesis(t *testing.T) {
    // Тест экспорта genesis
}

func TestAppBeginBlocker(t *testing.T) {
    // Тест BeginBlocker
}

func TestAppEndBlocker(t *testing.T) {
    // Тест EndBlocker
}
```

#### Целевое покрытие: 70%+

---

### 2.2 Добавить тесты для cmd/ модуля

#### Текущее состояние:
- ❌ Нет тестов для CLI команд

#### План тестирования:
```go
// cmd/volnixd/main_test.go
func TestRootCmd(t *testing.T) {
    // Тест root команды
}

func TestInitCmd(t *testing.T) {
    // Тест init команды
}

func TestStartCmd(t *testing.T) {
    // Тест start команды
}
```

#### Целевое покрытие: 60%+

---

### 2.3 Улучшить покрытие x/anteil/keeper

#### Текущее состояние:
- ✅ 57.9% покрытие
- ❌ Economic engine не покрыт

#### План:
```go
// x/anteil/keeper/economic_engine_test.go
func TestCalculateOrderPrice(t *testing.T) {
    // Тест расчета цены ордера
}

func TestMatchOrders(t *testing.T) {
    // Тест matching ордеров
}

func TestCalculateTradeFee(t *testing.T) {
    // Тест расчета комиссии
}

func TestUpdateMarketPrice(t *testing.T) {
    // Тест обновления рыночной цены
}
```

#### Целевое покрытие: 80%+

---

### 2.4 Улучшить покрытие x/ident/keeper

#### Текущее состояние:
- ✅ 46.4% покрытие
- ❌ ZKP верификация частично покрыта

#### План:
```go
// x/ident/keeper/zkp_verifier_test.go
func TestVerifyZKPProof(t *testing.T) {
    // Тест верификации ZKP доказательства
}

func TestGenerateZKPChallenge(t *testing.T) {
    // Тест генерации ZKP challenge
}

func TestValidateZKPResponse(t *testing.T) {
    // Тест валидации ZKP ответа
}
```

#### Целевое покрытие: 70%+

---

### 2.5 Добавить тесты для x/consensus/keeper

#### Текущее состояние:
- ✅ 55.9% покрытие
- ❌ Некоторые функции не покрыты

#### План:
```go
// Дополнительные тесты для consensus
func TestBurnAnt(t *testing.T) {
    // Тест сжигания ANT
}

func TestDistributeRewards(t *testing.T) {
    // Тест распределения наград
}

func TestUpdateValidatorSet(t *testing.T) {
    // Тест обновления набора валидаторов
}
```

#### Целевое покрытие: 80%+

---

### 2.6 Добавить тесты для x/governance/keeper

#### Текущее состояние:
- ✅ Есть базовые тесты
- ❌ Некоторые функции не покрыты

#### План:
```go
// Дополнительные тесты для governance
func TestExecuteProposal(t *testing.T) {
    // Тест выполнения предложения
}

func TestCancelProposal(t *testing.T) {
    // Тест отмены предложения
}

func TestDepositProposal(t *testing.T) {
    // Тест депозита для предложения
}
```

#### Целевое покрытие: 80%+

---

## 🎨 Приоритет 3: Оптимизация и расширение

### 3.1 Добавить edge cases тесты

#### План:
- Тесты с граничными значениями
- Тесты с невалидными входными данными
- Тесты с concurrent доступом
- Тесты с большими объемами данных

#### Примеры:
```go
func TestCreateOrder_EdgeCases(t *testing.T) {
    // Максимальная сумма
    // Минимальная сумма
    // Нулевая сумма
    // Отрицательная сумма
    // Очень большая сумма
}

func TestConcurrentOrderCreation(t *testing.T) {
    // Параллельное создание ордеров
    // Проверка race conditions
}
```

---

### 3.2 Улучшить integration тесты

#### План:
- Добавить больше сценариев
- Тестировать взаимодействие всех модулей
- Тестировать edge cases в полных сценариях

#### Новые сценарии:
```go
func TestComplexEconomicScenario(t *testing.T) {
    // Сложный экономический сценарий с множеством участников
}

func TestNetworkPartitionScenario(t *testing.T) {
    // Сценарий разделения сети
}

func TestValidatorRotationScenario(t *testing.T) {
    // Сценарий ротации валидаторов
}
```

---

### 3.3 Добавить performance тесты

#### План:
- Тесты производительности для критических операций
- Тесты масштабируемости
- Тесты использования памяти

#### Примеры:
```go
func BenchmarkLargeScaleTrading(b *testing.B) {
    // 10000 ордеров
    // 1000 сделок
    // Измерение времени
}

func BenchmarkMemoryUsage(b *testing.B) {
    // Измерение использования памяти
}
```

---

### 3.4 Добавить property-based тесты

#### План:
- Использовать библиотеку для property-based тестирования
- Тестировать инварианты системы
- Тестировать математические свойства

#### Примеры:
```go
// Используя gopter или similar
func TestOrderMatchingProperties(t *testing.T) {
    // Свойства matching ордеров
    // Инварианты системы
}
```

---

## 📊 Метрики и отслеживание прогресса

### Еженедельные метрики:
1. **Общее покрытие** - должно расти на 5-10% в неделю
2. **Количество падающих тестов** - должно уменьшаться
3. **Покрытие критических модулей** - должно быть >80%
4. **Время выполнения тестов** - должно быть <5 минут

### Инструменты:
```bash
# Генерация отчета покрытия
go test -coverprofile=coverage.out ./...
go tool cover -html=coverage.out -o coverage.html

# Отслеживание покрытия по модулям
go test -cover ./x/ident/keeper/...
go test -cover ./x/lizenz/keeper/...
go test -cover ./x/anteil/keeper/...
go test -cover ./x/consensus/keeper/...

# CI/CD интеграция
# Автоматический запуск тестов при каждом PR
```

---

## 🛠️ Инструменты и инфраструктура

### 1. Настроить CI/CD для тестов
```yaml
# .github/workflows/tests.yml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: 1.21
      - run: go test ./... -coverprofile=coverage.out
      - run: go tool cover -html=coverage.out -o coverage.html
      - uses: codecov/codecov-action@v2
        with:
          file: ./coverage.out
```

### 2. Настроить pre-commit hooks
```bash
# .git/hooks/pre-commit
#!/bin/bash
go test ./... -short
if [ $? -ne 0 ]; then
    echo "Tests failed. Commit aborted."
    exit 1
fi
```

### 3. Создать test helpers
```go
// tests/test_helpers.go
package tests

// Общие helper функции для всех тестов
func NewTestKeeper(...) *keeper.Keeper
func NewTestContext(...) sdk.Context
func NewTestAccount(...) *types.VerifiedAccount
```

---

## 📅 Временная шкала

### Неделя 1-2: Критические исправления
- ✅ Исправить все падающие тесты
- ✅ Исправить проблемы с store
- ✅ Исправить проблемы с контекстом
- **Результат:** 0 падающих тестов, стабильная инфраструктура

### Неделя 3-4: Улучшение покрытия keeper'ов
- ✅ Добавить тесты для economic engine
- ✅ Улучшить покрытие ZKP верификации
- ✅ Добавить тесты для consensus функций
- **Результат:** Покрытие keeper'ов >70%

### Неделя 5-6: Тесты для app и cmd
- ✅ Добавить тесты для app модуля
- ✅ Добавить тесты для cmd модуля
- **Результат:** Покрытие app >60%, cmd >60%

### Неделя 7-8: Edge cases и оптимизация
- ✅ Добавить edge cases тесты
- ✅ Улучшить integration тесты
- ✅ Добавить performance тесты
- **Результат:** Покрытие >80%, стабильные тесты

---

## ✅ Чеклист улучшений

### Критические исправления:
- [ ] Исправить 7 падающих msg_server тестов
- [ ] Исправить проблему "store does not exist" (13 тестов)
- [ ] Исправить проблему "Account limit exceeded"
- [ ] Исправить проблемы с контекстом в benchmark тестах

### Улучшение покрытия:
- [ ] Добавить тесты для app/ модуля (цель: 70%+)
- [ ] Добавить тесты для cmd/ модуля (цель: 60%+)
- [ ] Улучшить покрытие x/anteil/keeper до 80%+
- [ ] Улучшить покрытие x/ident/keeper до 70%+
- [ ] Улучшить покрытие x/consensus/keeper до 80%+
- [ ] Улучшить покрытие x/governance/keeper до 80%+

### Оптимизация:
- [ ] Добавить edge cases тесты
- [ ] Улучшить integration тесты
- [ ] Добавить performance тесты
- [ ] Добавить property-based тесты

### Инфраструктура:
- [ ] Настроить CI/CD для тестов
- [ ] Настроить pre-commit hooks
- [ ] Создать test helpers
- [ ] Настроить автоматическую генерацию отчетов

---

## 🎯 Целевые метрики

### Через 1 месяц:
- Общее покрытие: **30%+**
- Покрытие keeper'ов: **80%+**
- Падающие тесты: **0**
- Время выполнения тестов: **<3 минут**

### Через 2 месяца:
- Общее покрытие: **60%+**
- Покрытие всех модулей: **70%+**
- Критические функции: **100%**
- Время выполнения тестов: **<5 минут**

### Через 3 месяца:
- Общее покрытие: **80%+**
- Покрытие всех модулей: **80%+**
- Критические функции: **100%**
- Бизнес-логика: **95%+**
- Обработка ошибок: **90%+**

---

## 📝 Рекомендации

### 1. Приоритизация
- Начать с исправления падающих тестов
- Затем улучшить покрытие критических модулей
- Потом добавить тесты для непокрытых модулей

### 2. Инкрементальный подход
- Делать небольшие изменения
- Тестировать после каждого изменения
- Коммитить рабочие тесты

### 3. Документация
- Документировать все тесты
- Объяснять сложные тестовые сценарии
- Обновлять план при изменении приоритетов

### 4. Регулярный ревью
- Еженедельный ревью прогресса
- Обновление метрик
- Корректировка плана при необходимости

---

**Последнее обновление:** 9 ноября 2025

