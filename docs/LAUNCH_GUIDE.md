# 🚀 Руководство по запуску Волникс Протокол

## Обзор

**Волникс Протокол (Volnix Protocol)** - это инновационный блокчейн первого уровня, построенный на базе Cosmos SDK с уникальным консенсусом Proof-of-Verified-Burn (PoVB) и трехуровневой экономической моделью.

## 🎯 Цель руководства

Данное руководство поможет вам:
- Развернуть узел Волникс Протокол
- Настроить тестовую сеть
- Понять архитектуру и компоненты
- Подготовиться к mainnet

## 📋 Предварительные требования

### Системные требования
- **ОС**: Linux (Ubuntu 20.04+), macOS, Windows (WSL2)
- **CPU**: 2+ ядра
- **RAM**: 4GB+ (рекомендуется 8GB+)
- **Диск**: 100GB+ свободного места
- **Сеть**: Стабильное интернет-соединение

### Программное обеспечение
- **Go**: 1.21+ ([установка](https://golang.org/doc/install))
- **Git**: 2.20+ ([установка](https://git-scm.com/downloads))
- **Make**: 4.0+ (обычно предустановлен)

### Проверка установки
```bash
# Проверка Go
go version

# Проверка Git
git --version

# Проверка Make
make --version
```

## 🏗️ Архитектура проекта

### Компоненты
```
┌─────────────────────────────────────────────────────────┐
│                    Волникс Протокол                     │
├─────────────────────────────────────────────────────────┤
│  CLI (volnixd)  │  ABCI Server  │  Cosmos SDK App     │
├─────────────────────────────────────────────────────────┤
│  x/ident        │  x/lizenz      │  x/anteil           │
│  (ZKP роли)     │  (MOA)         │  (рынок ANT)        │
├─────────────────────────────────────────────────────────┤
│  x/consensus    │  GoLevelDB     │  Protobuf           │
│  (PoVB)         │  (хранилище)   │  (сериализация)     │
└─────────────────────────────────────────────────────────┘
```

### Модули
- **`x/ident`** - Управление ролями и ZKP верификация
- **`x/lizenz`** - Лицензии на майнинг и MOA проверки
- **`x/anteil`** - Внутренний рынок производительности ANT
- **`x/consensus`** - Консенсус Proof-of-Verified-Burn

## 🚀 Установка и сборка

### 1. Клонирование репозитория
```bash
# Клонирование
git clone https://github.com/volnix-protocol/volnix-protocol.git
cd volnix-protocol

# Проверка содержимого
ls -la
```

### 2. Установка зависимостей
```bash
# Обновление Go модулей
go mod tidy

# Проверка зависимостей
go mod verify
```

### 3. Сборка проекта
```bash
# Сборка через Makefile
make build

# Или ручная сборка
go build -ldflags "-s -w" -o bin/volnixd ./cmd/volnixd

# Проверка сборки
./bin/volnixd version
```

## ⚙️ Конфигурация

### 1. Инициализация узла
```bash
# Создание нового узла
./bin/volnixd init mynode

# Проверка созданных файлов
ls -la ~/.volnix/
ls -la ~/.volnix/config/
```

### 2. Настройка конфигурации
```bash
# Редактирование config.toml
nano ~/.volnix/config/config.toml

# Основные параметры:
# - moniker = "mynode"
# - chain_id = "test-volnix"
# - rpc_laddr = "tcp://0.0.0.0:26657"
# - p2p_laddr = "tcp://0.0.0.0:26656"
```

### 3. Настройка Genesis
```bash
# Редактирование genesis.json
nano ~/.volnix/config/genesis.json

# Проверка параметров:
# - chain_id: "test-volnix"
# - genesis_time: "2024-08-14T20:00:00Z"
# - bech32_prefix: "vx"
```

## 🚀 Запуск узла

### 1. Базовый запуск
```bash
# Запуск узла
./bin/volnixd start

# Проверка статуса
ps aux | grep volnixd
```

### 2. Запуск в фоне
```bash
# Запуск в фоне
nohup ./bin/volnixd start > volnix.log 2>&1 &

# Проверка логов
tail -f volnix.log
```

### 3. Запуск через systemd (Linux)
```bash
# Создание сервиса
sudo nano /etc/systemd/system/volnix.service

# Содержимое файла:
[Unit]
Description=Volnix Protocol Node
After=network.target

[Service]
Type=simple
User=volnix
WorkingDirectory=/home/volnix
ExecStart=/home/volnix/bin/volnixd start
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target

# Активация сервиса
sudo systemctl daemon-reload
sudo systemctl enable volnix
sudo systemctl start volnix
```

## 🌐 Тестовая сеть

### 1. Запуск тестсети
```bash
# Переход в папку тестсети
cd testnet

# Запуск
./start.sh

# Проверка статуса
make status
```

### 2. Подключение к существующей сети
```bash
# Добавление пиров в config.toml
persistent_peers = "node1@ip1:26656,node2@ip2:26656"

# Добавление сидов
seeds = "seed1@ip1:26656,seed2@ip2:26656"
```

### 3. Синхронизация
```bash
# Проверка синхронизации
curl http://localhost:26657/status

# Ожидание полной синхронизации
# (может занять несколько часов в зависимости от размера блокчейна)
```

## 🔧 Управление узлом

### 1. CLI команды
```bash
# Справка
./bin/volnixd --help

# Управление ключами
./bin/volnixd keys add mykey
./bin/volnixd keys list

# Транзакции
./bin/volnixd tx --help

# Запросы
./bin/volnixd query --help
```

### 2. Мониторинг
```bash
# Статус узла
curl http://localhost:26657/status

# Информация о сети
curl http://localhost:26657/net_info

# Последние блоки
curl http://localhost:26657/block?height=latest
```

### 3. Логи и отладка
```bash
# Просмотр логов
tail -f ~/.volnix/logs/volnix.log

# Уровень логирования в config.toml
log_level = "info"  # debug, info, warn, error
```

## 🔒 Безопасность

### 1. Сетевая безопасность
```bash
# Настройка файрвола
sudo ufw allow 26656/tcp  # P2P
sudo ufw allow 26657/tcp  # RPC
sudo ufw allow 1317/tcp   # API

# Ограничение доступа к RPC
# В config.toml изменить rpc_laddr на localhost
rpc_laddr = "tcp://127.0.0.1:26657"
```

### 2. Ключи и валидация
```bash
# Безопасное хранение ключей
./bin/volnixd keys export mykey --unsafe-unarmored-hex

# Резервное копирование
cp ~/.volnix/config/priv_validator_key.json ~/backup/
cp ~/.volnix/config/node_key.json ~/backup/
```

### 3. Обновления
```bash
# Проверка новых версий
git fetch origin
git checkout main
git pull origin main

# Пересборка
make clean
make build

# Перезапуск узла
sudo systemctl restart volnix
```

## 📊 Мониторинг и метрики

### 1. Prometheus метрики
```bash
# Включение метрик в config.toml
[instrumentation]
prometheus = true
prometheus_listen_addr = ":26660"

# Настройка Prometheus
# targets: ['localhost:26660']
# metrics_path: '/metrics'
```

### 2. Grafana дашборды
```bash
# Установка Grafana
sudo apt install grafana

# Импорт дашборда Cosmos SDK
# (доступны готовые шаблоны)
```

### 3. Алерты
```bash
# Настройка алертов для:
# - Высокого использования CPU/RAM
# - Остановки узла
# - Отставания в синхронизации
# - Ошибок в логах
```

## 🚧 Устранение неполадок

### 1. Узел не запускается
```bash
# Проверка логов
tail -f ~/.volnix/logs/volnix.log

# Проверка портов
netstat -tlnp | grep :26657

# Проверка прав доступа
ls -la ~/.volnix/
```

### 2. Проблемы с синхронизацией
```bash
# Проверка статуса
curl http://localhost:26657/status

# Сброс состояния (осторожно!)
./bin/volnixd unsafe-reset-all

# Проверка подключения к пирам
curl http://localhost:26657/net_info
```

### 3. Высокое использование ресурсов
```bash
# Мониторинг ресурсов
htop
iotop

# Оптимизация в config.toml
max_num_inbound_peers = 20
max_num_outbound_peers = 10
```

## 🔮 Следующие шаги

### 1. Валидация
```bash
# Создание валидатора
./bin/volnixd tx staking create-validator \
  --amount=1000000uvx \
  --pubkey=$(./bin/volnixd tendermint show-validator) \
  --moniker="mynode" \
  --chain-id=test-volnix \
  --commission-rate="0.10" \
  --commission-max-rate="0.20" \
  --commission-max-change-rate="0.01" \
  --min-self-delegation="1" \
  --gas="auto" \
  --gas-adjustment=1.4 \
  --from=mykey
```

### 2. Делегирование
```bash
# Делегирование токенов
./bin/volnixd tx staking delegate \
  volnixvaloper1... \
  1000000uvx \
  --chain-id=test-volnix \
  --gas="auto" \
  --gas-adjustment=1.4 \
  --from=mykey
```

### 3. Участие в управлении
```bash
# Голосование за предложения
./bin/volnixd tx gov vote 1 yes \
  --chain-id=test-volnix \
  --gas="auto" \
  --gas-adjustment=1.4 \
  --from=mykey
```

## 📚 Дополнительные ресурсы

### Документация
- [Cosmos SDK Documentation](https://docs.cosmos.network/)
- [CometBFT Documentation](https://docs.cometbft.com/)
- [Go Documentation](https://golang.org/doc/)

### Сообщество
- [GitHub Issues](https://github.com/volnix-protocol/issues)
- [GitHub Discussions](https://github.com/volnix-protocol/discussions)
- [Discord/Telegram] (ссылки будут добавлены)

### Инструменты
- [Cosmos Hub Explorer](https://www.mintscan.io/cosmos)
- [Big Dipper](https://github.com/forbole/big-dipper)
- [Lunie](https://github.com/luniehq/lunie)

## 🎉 Заключение

Поздравляем! Вы успешно развернули узел Волникс Протокол и готовы участвовать в экосистеме.

### Что дальше?
1. **Изучите экономическую модель** - WRT, LZN, ANT токены
2. **Поняйте консенсус PoVB** - уникальный подход к валидации
3. **Исследуйте ZKP интеграцию** - безопасность через криптографию
4. **Присоединяйтесь к сообществу** - делитесь опытом и помогайте другим

### Поддержка
Если у вас возникли вопросы или проблемы:
1. Проверьте данное руководство
2. Изучите документацию проекта
3. Создайте Issue на GitHub
4. Обратитесь к сообществу

---

**Волникс Протокол** - Строим будущее децентрализованной экономики! 🚀

*Версия документа: 1.0*
*Последнее обновление: $(date)*
