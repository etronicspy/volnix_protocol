# 🚀 Руководство по запуску Helvetia Protocol

## Обзор

Это руководство описывает, как запустить Helvetia Protocol (H•P) - суверенный блокчейн первого уровня на базе Cosmos SDK.

## 📋 Текущий статус

### ✅ Что готово к запуску
- **Базовая архитектура** Cosmos SDK
- **Кастомные модули** (ident, lizenz, anteil, consensus)
- **CLI команды** для управления узлом
- **Персистентное хранилище** (GoLevelDB)
- **Genesis и конфигурация**
- **ABCI сервер** с сохранением состояния

### 🔄 Что в разработке
- **Полная интеграция с CometBFT**
- **ZKP верификация**
- **Экономическая логика**
- **Консенсус PoVB**

## 🛠️ Предварительные требования

### Системные требования
- **ОС**: Linux, macOS, Windows (WSL)
- **Go**: 1.24+
- **RAM**: Минимум 2GB
- **Диск**: Минимум 10GB свободного места

### Установка Go
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install golang-go

# macOS
brew install go

# Проверка версии
go version
```

### Установка buf CLI
```bash
curl -sSL https://buf.build/install.sh | bash -s -- -b /usr/local/bin
buf --version
```

## 🚀 Пошаговый запуск

### Шаг 1: Клонирование и настройка
```bash
# Клонирование репозитория
git clone <repository-url>
cd helvetia-protocol

# Установка зависимостей
go mod tidy

# Сборка проекта
make build

# Проверка сборки
./bin/volnixd version
```

### Шаг 2: Инициализация узла
```bash
# Создание нового узла
./bin/volnixd init mynode

# Проверка созданных файлов
ls -la ~/.volnix/
ls -la ~/.volnix/config/
```

**Создаваемые файлы:**
- `~/.volnix/config/genesis.json` - Начальное состояние блокчейна
- `~/.volnix/config/config.toml` - Конфигурация узла
- `~/.volnix/data/` - Директория для данных (создается при первом запуске)

### Шаг 3: Запуск узла
```bash
# Запуск с персистентным хранилищем
./bin/volnixd start
```

**Что происходит при запуске:**
1. Создается персистентная БД (GoLevelDB)
2. Загружается последняя версия состояния
3. Запускается ABCI сервер
4. Узел готов к работе

### Шаг 4: Тестовая сеть (опционально)
```bash
# Переход в директорию тестовой сети
cd testnet

# Запуск тестовой сети
./start.sh
```

## 🔧 Конфигурация

### Genesis параметры
```json
{
  "chain_id": "test-volnix",
  "genesis_time": "2024-08-14T20:00:00Z",
  "consensus_params": {
    "block": {
      "max_bytes": "22020096",
      "max_gas": "-1",
      "time_iota_ms": "1000"
    }
  }
}
```

### Bech32 префиксы
- **Адреса**: `vx` (например: `vx1abc...`)
- **Валидаторы**: `vxvaloper` (например: `vxvaloper1abc...`)
- **Консенсус**: `vxvalcons` (например: `vxvalcons1abc...`)

### Порты
- **RPC**: 26657 (HTTP API)
- **P2P**: 26656 (P2P соединения)
- **ABCI**: 26658 (ABCI приложение)

## 📊 Мониторинг и управление

### Проверка статуса
```bash
# Проверка запущенных процессов
ps aux | grep volnixd

# Проверка портов
netstat -tlnp | grep 26657
netstat -tlnp | grep 26656
```

### Остановка узла
```bash
# Graceful остановка
pkill -f volnixd

# Принудительная остановка
pkill -9 -f volnixd
```

### Просмотр логов
```bash
# Логи выводятся в stdout при запуске
# Для продакшена рекомендуется настроить ротацию логов
```

## 🧪 Тестирование

### CLI команды
```bash
# Версия
./bin/volnixd version

# Справка
./bin/volnixd --help

# Управление ключами
./bin/volnixd keys add mykey
./bin/volnixd keys list

# Транзакции (заглушки)
./bin/volnixd tx ident verify-identity vx1abc... citizen
./bin/volnixd tx lizenz activate vxvaloper1abc... 1000000
./bin/volnixd tx anteil place-order vx1abc... limit buy 1000 1.5
```

### Запросы (заглушки)
```bash
# Запросы к модулям
./bin/volnixd query ident account vx1abc...
./bin/volnixd query lizenz activated vxvaloper1abc...
./bin/volnixd query anteil orderbook
```

## 🔒 Безопасность

### Рекомендации
- **Не используйте** реальные приватные ключи в тестовой сети
- **Ограничьте доступ** к RPC порту (26657) в продакшене
- **Настройте firewall** для P2P порта (26656)
- **Регулярно обновляйте** зависимости

### Файлы безопасности
- `~/.volnix/config/node_key.json` - Ключ узла
- `~/.volnix/config/priv_validator_key.json` - Приватный ключ валидатора
- `~/.volnix/keys/` - Пользовательские ключи

## 🚧 Устранение неполадок

### Частые проблемы

#### 1. Узел не запускается
```bash
# Проверьте, что узел инициализирован
ls -la ~/.volnix/config/

# Проверьте права доступа
chmod 755 ~/.volnix/
chmod 644 ~/.volnix/config/*

# Проверьте свободное место на диске
df -h
```

#### 2. Порт уже занят
```bash
# Найдите процесс, использующий порт
lsof -i :26657
lsof -i :26656

# Остановите конфликтующий процесс
pkill -f <process_name>
```

#### 3. Ошибки БД
```bash
# Удалите поврежденную БД (осторожно!)
rm -rf ~/.volnix/data/

# Переинициализируйте узел
./bin/volnixd init mynode
```

### Логи и отладка
```bash
# Запуск с подробным логированием
./bin/volnixd start --log_level debug

# Проверка конфигурации
./bin/volnixd start --help
```

## 🔮 Следующие шаги

### Для разработчиков
1. **Изучите модули** в директории `x/`
2. **Добавьте тесты** для ваших изменений
3. **Обновите protobuf** при изменении типов
4. **Следуйте стандартам** Cosmos SDK

### Для валидаторов
1. **Настройте мониторинг** (Prometheus, Grafana)
2. **Настройте backup** состояния
3. **Подготовьтесь к mainnet**
4. **Изучите экономическую модель**

### Для пользователей
1. **Создайте кошелек** и ключи
2. **Изучите токеномику** (WRT, LZN, ANT)
3. **Поняйте роли** (Гость, Гражданин, Валидатор)
4. **Изучите рынок ANT**

## 📚 Дополнительные ресурсы

### Документация
- [Cosmos SDK Documentation](https://docs.cosmos.network/)
- [CometBFT Documentation](https://docs.cometbft.com/)
- [Protobuf Guide](https://developers.google.com/protocol-buffers)

### Сообщество
- [GitHub Issues](https://github.com/volnix-protocol/volnix-protocol/issues)
- [GitHub Discussions](https://github.com/volnix-protocol/volnix-protocol/discussions)
- [Discord/Telegram] (ссылки будут добавлены)

## 🎉 Поздравляем!

**Вы успешно запустили Helvetia Protocol!** 

Теперь у вас есть:
- ✅ Рабочий узел с персистентным хранилищем
- ✅ ABCI сервер для обработки транзакций
- ✅ CLI для управления и тестирования
- ✅ Базовая инфраструктура для разработки

**Следующий этап**: Интеграция с CometBFT для создания полноценного блокчейна с консенсусом.

---

**Helvetia Protocol** - Строим будущее децентрализованной экономики! 🚀
