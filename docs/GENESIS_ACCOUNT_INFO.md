# 🔑 Genesis Аккаунт - Информация

## 📋 Данные для подключения

**Адрес:** `volnix19rl4cm2hmr8afy4kldpxz3fka4jguq0a9r0ces`

**Мнемоника:** 
```
abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about
```

**Балансы:**
- 1000 WRT (1000000000 uwrt)
- 1000 LZN (1000000000 ulzn)
- 1000 ANT (1000000000 uant)

---

## 🚀 Как использовать

### Шаг 1: Пересобрать узел

```bash
go build -o build/volnixd-standalone ./cmd/volnixd-standalone
```

Или используйте Makefile:
```bash
make build-standalone
```

### Шаг 2: Перезапустить узел

Остановите текущий узел и запустите новый бинарник.

### Шаг 3: Подключить кошелек в UI

1. Откройте UI кошелька (http://localhost:3000)
2. Нажмите **"Connect Wallet"**
3. Выберите **"Connect with Mnemonic"**
4. Введите мнемонику:
   ```
   abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about
   ```
5. Нажмите **"Connect"**

### Шаг 4: Проверить балансы

После подключения вы должны увидеть:
- **1000 WRT**
- **1000 LZN**
- **1000 ANT**

### Шаг 5: Отправить токены

**Вариант A: Через UI**
1. Откройте вкладку **"Send"**
2. Введите адрес получателя
3. Выберите токен (WRT, LZN, или ANT)
4. Введите количество (например, 100)
5. Нажмите **"Send"**

**Вариант B: Через скрипт**
```bash
cd frontend/wallet-ui
export SENDER_MNEMONIC='abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about'
npx ts-node scripts/send-tokens.ts
```

---

## 📝 Адреса получателей

Для отправки токенов используйте эти адреса:

1. `vo1n1x18xxeuuqd37xtp52luuqpw3acfw0cgk3vvea3v`
2. `vo1nix19tvhq59sfffvm37cm0d9pkf6jyl3sn7ev5try9q`
3. `volnix1kfm2jun5v4lacd4xrzpnsepm7y0eesrmf3e41r`

---

## ⚠️ Важно

- **Не используйте эту мнемонику в продакшене!** Это тестовая мнемоника для разработки.
- Genesis аккаунт создан только в коде узла (Query метод возвращает балансы для этого адреса).
- После перезапуска узла балансы будут доступны сразу.

---

## 🔧 Технические детали

Genesis аккаунт реализован в `cmd/volnixd-standalone/main.go`:
- Query метод проверяет запрашиваемый адрес
- Если адрес совпадает с genesis адресом, возвращает балансы
- Балансы возвращаются в protobuf формате для совместимости с CosmJS

