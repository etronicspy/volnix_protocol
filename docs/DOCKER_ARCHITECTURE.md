# 🐳 Docker Architecture в Volnix Protocol

> **Этот документ описывает архитектуру Docker образа для одного валидатора**
> 
> Для инструкций по развертыванию см. [DOCKER_DEPLOYMENT.md](DOCKER_DEPLOYMENT.md)  
> Для локального тестирования сети см. `testnet/docker-compose.test.yml`

## 📦 Что упаковывается в Docker образ

### 1. Основной образ валидатора (`Dockerfile`)

#### Stage 1: Build (Builder)
**Базовый образ:** `golang:1.21-alpine`

**Что происходит:**
1. **Установка зависимостей сборки:**
   - Git, Make, GCC, musl-dev, linux-headers
   - Инструменты безопасности: `golangci-lint`, `govulncheck`

2. **Копирование исходного кода:**
   ```
   /app/
   ├── go.mod, go.sum          # Зависимости Go
   ├── cmd/volnixd-standalone/  # Исходный код приложения
   ├── app/                     # Логика приложения (ABCI, сервер)
   ├── x/                       # Модули (ident, lizenz, anteil, consensus)
   ├── proto/                   # Protobuf определения
   └── gen/                     # Сгенерированный код
   ```

3. **Сборка бинарника:**
   - Компиляция `volnixd-standalone` для Linux (amd64)
   - Статическая линковка (CGO_ENABLED=0)
   - Встраивание версии, коммита, времени сборки
   - Результат: `/app/build/volnixd-standalone` (статический бинарник ~20-30MB)

#### Stage 2: Runtime (Production)
**Базовый образ:** `alpine:3.18` (~5MB)

**Что упаковывается:**
1. **Минимальный runtime:**
   - CA сертификаты для HTTPS
   - Bash для скриптов
   - Curl для health checks и API запросов
   - jq для работы с JSON (парсинг конфигурации, genesis)
   - netcat для проверки сетевых соединений
   - Timezone данные

2. **Бинарник приложения:**
   ```
   /usr/local/bin/volnixd-standalone  # Основной исполняемый файл
   ```

3. **Утилиты и скрипты:**
   ```
   /usr/local/bin/entrypoint.sh      # Entrypoint для автоматической инициализации
   /usr/local/bin/healthcheck.sh     # Health check скрипт
   /usr/local/bin/node-info.sh        # Утилита для получения информации о узле
   ```

3. **Структура директорий:**
   ```
   /home/volnix/
   └── .volnix/
       ├── config/              # Конфигурация (genesis.json, config.toml)
       └── data/                 # Блокчейн данные (БД, блоки, состояние)
   ```

**Итоговый размер образа:** ~35-45MB (компактный благодаря multi-stage build, включает необходимые утилиты)

**Автоматическая инициализация:**
- При первом запуске контейнера автоматически выполняется `volnixd-standalone init`
- Создаются необходимые директории и конфигурационные файлы
- Genesis и config.toml генерируются автоматически
- Поддержка кастомных параметров через environment variables (MONIKER, CHAIN_ID)

---

## 🏗️ Архитектура Docker Compose

### ⚠️ Важное понимание архитектуры

**Каждый валидатор = отдельный Docker контейнер**

В production сети каждый валидатор запускается в **своем собственном Docker контейнере**. Сеть формируется из множества таких контейнеров, которые могут быть развернуты:
- На разных серверах
- В разных дата-центрах  
- В разных облачных провайдерах

**docker-compose.yml** с 3 валидаторами - это **пример минимальной конфигурации** для тестирования или небольшой сети. В реальной production сети может быть десятки или сотни валидаторов, каждый в своем контейнере.

**Важно:** Сеть формируется из **отдельных узлов в отдельных контейнерах**, а не из одного скрипта. Каждый валидатор - независимый контейнер со своим volume, конфигурацией и данными.

### Сетевая топология (пример с 3 валидаторами)

```
┌─────────────────────────────────────────────────────────────┐
│              Docker Network: volnix-network                 │
│              Subnet: 172.28.0.0/16                          │
│                                                             │
│  ⚠️  Каждый валидатор - ОТДЕЛЬНЫЙ контейнер                 │
│  ⚠️  В production сети может быть N валидаторов             │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼───────┐  ┌────────▼────────┐  ┌──────▼───────┐
│ validator-0  │  │  validator-1    │  │ validator-2  │
│ Container    │  │  Container      │  │ Container    │
│              │  │                 │  │             │
│ P2P: 26656   │◄─┤ P2P: 26656      │◄─┤ P2P: 26656  │
│ RPC: 26657   │  │ RPC: 26657      │  │ RPC: 26657  │
│              │  │                 │  │             │
│ Volume:      │  │ Volume:         │  │ Volume:      │
│ validator-0  │  │ validator-1     │  │ validator-2  │
└──────────────┘  └─────────────────┘  └─────────────┘
        │
        │ (метрики)
        │
┌───────▼───────┐  ┌──────────────┐
│  Prometheus   │◄─┤   Grafana    │
│  Container    │  │  Container   │
│  Port: 9090   │  │  Port: 3001  │
└───────────────┘  └──────────────┘
```

### Production архитектура сети

В реальной production сети:

```
┌─────────────────────────────────────────────────────────────┐
│                    Volnix Protocol Network                  │
│                                                             │
│  Валидатор 1 (Server 1)     Валидатор 2 (Server 2)          │
│  ┌──────────────┐            ┌──────────────┐               │
│  │ Container    │            │ Container    │               │
│  │ validator-1  │◄──────────►│ validator-2  │               │
│  │              │   P2P      │              │               │
│  └──────────────┘            └──────────────┘               │
│                                                             │
│  Валидатор 3 (Server 3)     Валидатор N (Server N)          │
│  ┌──────────────┐            ┌──────────────┐               │
│  │ Container    │            │ Container    │               │
│  │ validator-3  │◄──────────►│ validator-N  │               │
│  │              │   P2P      │              │               │
│  └──────────────┘            └──────────────┘               │
│                                                             │
│  Каждый валидатор:                                          │
│  - Отдельный Docker контейнер                                │
│  - Свой volume для данных                                    │
│  - Свой IP адрес                                             │
│  - Может быть на разных серверах/дата-центрах               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🌐 Как формируется сеть в Production

### Ключевой принцип: Один валидатор = Один контейнер

**Важно понимать:**
- Сеть **НЕ формируется из одного скрипта или docker-compose файла**
- Сеть формируется из **множества независимых Docker контейнеров**
- Каждый валидатор запускается в **своем собственном контейнере**
- Контейнеры могут быть на **разных серверах, в разных дата-центрах**

### Пример Production развертывания:

```
┌─────────────────────────────────────────────────────────────┐
│              Volnix Protocol Production Network             │
│                                                             │
│  Server 1 (US-East)                                         │
│  ┌─────────────────────────────────────┐                   │
│  │ Docker Container: validator-1        │                   │
│  │ - Image: volnix-validator:latest     │                   │
│  │ - Volume: validator-1-data           │                   │
│  │ - IP: 1.2.3.4                        │                   │
│  │ - P2P: 1.2.3.4:26656                 │                   │
│  └─────────────────────────────────────┘                   │
│                                                             │
│  Server 2 (EU-West)                                         │
│  ┌─────────────────────────────────────┐                   │
│  │ Docker Container: validator-2        │                   │
│  │ - Image: volnix-validator:latest   │                   │
│  │ - Volume: validator-2-data           │                   │
│  │ - IP: 5.6.7.8                        │                   │
│  │ - P2P: 5.6.7.8:26656                 │                   │
│  └─────────────────────────────────────┘                   │
│                                                             │
│  Server 3 (Asia-Pacific)                                    │
│  ┌─────────────────────────────────────┐                   │
│  │ Docker Container: validator-3        │                   │
│  │ - Image: volnix-validator:latest   │                   │
│  │ - Volume: validator-3-data           │                   │
│  │ - IP: 9.10.11.12                    │                   │
│  │ - P2P: 9.10.11.12:26656             │                   │
│  └─────────────────────────────────────┘                   │
│                                                             │
│  Все контейнеры подключаются через P2P сеть                │
│  используя реальные IP адреса                               │
└─────────────────────────────────────────────────────────────┘
```

### docker-compose.yml - это пример для тестирования

**docker-compose.yml** с 3 валидаторами используется для:
- ✅ Локального тестирования
- ✅ Разработки
- ✅ Небольших тестовых сетей

**НЕ используется для:**
- ❌ Production сети (каждый валидатор должен быть на своем сервере)
- ❌ Больших сетей (используйте оркестрацию: Kubernetes, Docker Swarm)

### Развертывание валидатора в Production:

```bash
# На каждом сервере валидатора
docker run -d \
  --name volnix-validator-1 \
  --restart unless-stopped \
  -v validator-1-data:/home/volnix/.volnix/data \
  -v validator-1-config:/home/volnix/.volnix/config:ro \
  -p 26656:26656 \
  -p 26657:26657 \
  -e MONIKER=validator-1 \
  -e CHAIN_ID=volnix-mainnet \
  volnix-validator:latest
```

Каждый валидатор:
- Имеет свой уникальный volume
- Имеет свой IP адрес
- Подключается к другим валидаторам через P2P (реальные IP)
- Работает независимо от других валидаторов

---

## 🔧 Компоненты Docker образа

### Validator Node Container

**Один Docker образ = один валидатор**

Каждый контейнер содержит:

#### Что запускается в контейнере:

**Основной процесс:** `volnixd-standalone start`

**Компоненты внутри контейнера:**

1. **CometBFT Node** (консенсус-слой):
   - **P2P сервер** (порт 26656): Обмен блоками и сообщениями консенсуса
   - **RPC сервер** (порт 26657): HTTP API для запросов и транзакций
   - **ABCI клиент**: Связь с приложением через ABCI протокол
   - **Mempool**: Хранение неподтвержденных транзакций
   - **Consensus Engine**: Алгоритм консенсуса (PoVB)

2. **Volnix Application** (прикладной слой):
   - **ABCI Server**: Обработка запросов от CometBFT
   - **State Machine**: Управление состоянием блокчейна
   - **Modules**:
     - `ident`: Верификация идентичности (ZKP)
     - `lizenz`: Управление лицензиями (LZN)
     - `anteil`: Внутренний рынок (ANT)
     - `consensus`: PoVB консенсус механизм
     - `governance`: Управление сетью

3. **Database** (GoLevelDB):
   - `blockstore.db`: Хранение блоков
   - `state.db`: Состояние приложения
   - `tx_index.db`: Индекс транзакций
   - `application.db`: Данные модулей

#### Данные в контейнере:

**Volume: `validator-X-data`** (блокчейн данные):
```
/home/volnix/.volnix/data/
├── blockstore.db/          # Блоки блокчейна
├── state.db/               # Состояние приложения
├── tx_index.db/            # Индекс транзакций
├── application.db/         # Данные модулей
└── priv_validator_state.json  # Состояние валидатора
```

**Volume: `validator-X-config`** (конфигурация, read-only):
```
/home/volnix/.volnix/config/
├── genesis.json            # Генезис блокчейна
├── config.toml             # Конфигурация CometBFT
├── node_key.json           # P2P ключ узла
└── priv_validator_key.json # Ключ валидатора
```

#### Сетевые порты:

- **26656 (P2P)**: Межузловое взаимодействие
  - Обмен блоками
  - Сообщения консенсуса (prevote, precommit)
  - Распространение транзакций
  - Peer discovery

- **26657 (RPC)**: HTTP API
  - `/status` - статус узла
  - `/broadcast_tx_*` - отправка транзакций
  - `/abci_query` - запросы к состоянию
  - `/block` - получение блоков
  - `/tx_search` - поиск транзакций

#### Взаимодействие с другими валидаторами:

1. **P2P соединения:**
   ```toml
   # config.toml вашего валидатора
   [p2p]
   persistent_peers = "node_id_1@validator-1.example.com:26656,node_id_2@validator-2.example.com:26656"
   seeds = "seed1.volnix.network:26656,seed2.volnix.network:26656"
   ```
   - Валидаторы подключаются друг к другу через **реальные IP адреса**
   - Используют порт 26656 для P2P соединений
   - RPC порт 26657 для доступа с хоста

2. **Консенсус процесс:**
   ```
   Validator-0 → создает блок → отправляет через P2P
   ↓
   Validator-1, Validator-2 → получают блок → валидируют
   ↓
   Все валидаторы → голосуют (prevote/precommit)
   ↓
   Блок финализируется → записывается в БД всех валидаторов
   ```

3. **Синхронизация состояния:**
   - При запуске валидатор проверяет высоту блока
   - Если отстает - запрашивает блоки у других валидаторов
   - Синхронизирует состояние через ABCI запросы


---

## 🔄 Жизненный цикл контейнера

### Запуск одного валидатора:

```bash
docker run -d \
  --name volnix-validator \
  --restart unless-stopped \
  -p 26656:26656 \
  -p 26657:26657 \
  -v volnix-validator-data:/home/volnix/.volnix/data \
  -v volnix-validator-config:/home/volnix/.volnix/config:ro \
  -e MONIKER=my-validator \
  -e CHAIN_ID=volnix-mainnet \
  volnix-validator:latest
```

**Последовательность запуска:**

1. **Entrypoint скрипт** (`entrypoint.sh`) проверяет инициализацию
   - Если узел не инициализирован → автоматически выполняет `volnixd-standalone init`
   - Создает необходимые директории и конфигурационные файлы
   - Генерирует ключи валидатора (если отсутствуют)

2. **Запуск узла** (`volnixd-standalone start`)
   - CometBFT инициализирует консенсус
   - Volnix Application запускает модули
   - Узел начинает слушать P2P и RPC порты

3. **Подключение к сети**
   - Узел подключается к другим валидаторам через `persistent_peers` (из config.toml)
   - Синхронизирует состояние с сетью
   - Присоединяется к консенсусу

4. **Health checks**
   - Docker автоматически проверяет здоровье через `healthcheck.sh`
   - Проверка доступности RPC endpoint каждые 30 секунд

### Работа валидатора в сети:

**Нормальная работа:**
```
1. Клиент → ваш-валидатор RPC → отправляет транзакцию
2. Ваш валидатор → транзакция попадает в mempool
3. Ваш валидатор → P2P → распространяет транзакцию другим валидаторам
4. Все валидаторы → получают транзакцию
5. Валидатор-создатель-блока → создает блок с транзакцией
6. Валидатор-создатель-блока → P2P → отправляет блок
7. Все валидаторы (включая ваш) → валидируют блок → голосуют
8. Блок финализируется → записывается в БД всех валидаторов
9. Состояние обновляется на всех узлах (включая ваш)
```

**Синхронизация:**
- Если ваш валидатор отстает, он автоматически запрашивает блоки у других
- CometBFT автоматически синхронизирует состояние через P2P
- После синхронизации валидатор присоединяется к консенсусу

### Остановка:

```bash
docker-compose stop
```

- Graceful shutdown: SIGTERM → приложение корректно завершает работу
- Состояние сохраняется в volumes
- При следующем запуске продолжает с сохраненного состояния

---

## 💾 Управление данными

### Volumes структура (для одного валидатора):

```
Docker Volumes (на хосте):
├── volnix-validator-data/   # Блокчейн данные вашего валидатора
│   ├── blockstore.db/        # Блоки блокчейна
│   ├── state.db/             # Состояние приложения
│   ├── tx_index.db/          # Индекс транзакций
│   └── application.db/       # Данные модулей
└── volnix-validator-config/  # Конфигурация вашего валидатора (read-only)
    ├── genesis.json          # Генезис блокчейна
    ├── config.toml           # Конфигурация CometBFT
    ├── node_key.json         # P2P ключ узла
    └── priv_validator_key.json # Ключ валидатора
```

### Персистентность:

- **Блокчейн данные**: Сохраняются между перезапусками
- **Конфигурация**: Read-only, обновляется через volumes
- **Метрики**: Хранятся 30 дней в Prometheus
- **Дашборды**: Сохраняются в Grafana volume

### Backup стратегия:

1. **Блокчейн данные**: Критично для восстановления
2. **Конфигурация**: Может быть пересоздана
3. **Метрики**: Опционально (можно пересобрать)
4. **Grafana**: Дашборды в git, данные опциональны

---

## 🛠️ Утилиты и инструменты в контейнере

### Встроенные скрипты

#### `entrypoint.sh`
**Назначение:** Автоматическая инициализация и запуск узла

**Функции:**
- Проверяет, инициализирован ли узел
- Автоматически выполняет `volnixd-standalone init` при первом запуске
- Создает необходимые директории
- Обновляет chain_id в genesis.json при необходимости
- Передает все аргументы команде `volnixd-standalone`

**Использование:**
```bash
# Автоматически вызывается при запуске контейнера
docker-compose up -d validator-0

# Или вручную с параметрами
docker run -e MONIKER=my-node -e CHAIN_ID=my-chain volnix-validator
```

#### `healthcheck.sh`
**Назначение:** Проверка здоровья узла

**Функции:**
- Проверяет доступность RPC endpoint
- Используется Docker healthcheck механизмом
- Возвращает код 0 при успехе, 1 при ошибке

**Использование:**
```bash
# Автоматически вызывается Docker
# Или вручную:
docker exec volnix-validator healthcheck.sh
```

#### `node-info.sh`
**Назначение:** Получение информации о узле

**Функции:**
- Показывает конфигурацию узла
- Отображает информацию из genesis.json
- Получает статус через RPC API
- Показывает информацию о блокчейне

**Использование:**
```bash
docker exec volnix-validator node-info.sh
```

### Установленные утилиты

- **jq**: Парсинг и обработка JSON (конфигурация, genesis, RPC ответы)
- **curl**: HTTP запросы к RPC API
- **netcat**: Проверка сетевых соединений
- **bash**: Выполнение скриптов

### Примеры использования

```bash
# Получить информацию о узле
docker exec volnix-validator node-info.sh

# Проверить конфигурацию
docker exec volnix-validator cat /home/volnix/.volnix/config/config.toml | jq

# Получить статус через RPC
docker exec volnix-validator curl -s http://localhost:26657/status | jq

# Проверить высоту блока
docker exec volnix-validator curl -s http://localhost:26657/status | jq -r '.result.sync_info.latest_block_height'

# Проверить валидаторов
docker exec volnix-validator curl -s http://localhost:26657/validators | jq

# Проверить P2P соединения
docker exec volnix-validator curl -s http://localhost:26657/net_info | jq '.result.peers | length'
```

### Environment Variables

**VOLNIX_HOME** (по умолчанию: `/home/volnix/.volnix`)
- Домашняя директория узла
- Содержит config/ и data/ поддиректории

**MONIKER** (по умолчанию: `validator`)
- Имя узла (используется при инициализации)
- Отображается в статусе и логах

**CHAIN_ID** (по умолчанию: `volnix-standalone`)
- Идентификатор блокчейна
- Обновляется в genesis.json при инициализации

**VOLNIX_RPC_PORT** (по умолчанию: `26657`)
- Порт RPC сервера
- Используется для health checks

**VOLNIX_P2P_PORT** (по умолчанию: `26656`)
- Порт P2P сервера
- Используется для межузлового взаимодействия

---

## 🔐 Безопасность

### Изоляция:

- **Network isolation**: Все сервисы в одной Docker network
- **User isolation**: Запуск от non-root пользователя (volnix:1000)
- **Read-only config**: Конфигурация монтируется read-only
- **Resource limits**: CPU и память ограничены

### Ключи и секреты:

**В контейнере:**
- `priv_validator_key.json`: Ключ валидатора (в config volume)
- `node_key.json`: P2P ключ узла (в config volume)

**Безопасность:**
- Ключи хранятся в Docker volumes (не в образе)
- Конфигурация read-only (не может быть изменена из контейнера)
- Приватные ключи никогда не покидают контейнер

---

## 📊 Мониторинг и отладка

### Логи:

```bash
# Все сервисы
docker-compose logs -f

# Конкретный валидатор
docker-compose logs -f validator-0

# Последние 100 строк
docker-compose logs --tail=100 validator-0
```

### Метрики:

- **RPC Status**: `curl http://localhost:26657/status`
- **Node Info**: `docker exec volnix-validator node-info.sh`
- **Block Height**: `curl -s http://localhost:26657/status | jq -r '.result.sync_info.latest_block_height'`

### Отладка:

```bash
# Войти в контейнер
docker exec volnix-validator sh

# Проверить конфигурацию
cat /home/volnix/.volnix/config/config.toml

# Проверить состояние
curl http://localhost:26657/status | jq

# Проверить P2P соединения
curl http://localhost:26657/net_info | jq
```

---

## 🚀 Развертывание в Production

### Запуск одного валидатора

**Базовый запуск:**
```bash
docker run -d \
  --name volnix-validator \
  --restart unless-stopped \
  -p 26656:26656 \
  -p 26657:26657 \
  -v volnix-validator-data:/home/volnix/.volnix/data \
  -v volnix-validator-config:/home/volnix/.volnix/config:ro \
  -e MONIKER=my-validator \
  -e CHAIN_ID=volnix-mainnet \
  volnix-validator:latest
```

### Подключение к существующей сети

1. **Получите список persistent_peers** от других валидаторов
2. **Настройте config.toml** (через volume или при монтировании):
   ```toml
   [p2p]
   persistent_peers = "node_id1@ip1:26656,node_id2@ip2:26656"
   seeds = "seed1.volnix.network:26656,seed2.volnix.network:26656"
   ```
3. **Убедитесь, что genesis.json совпадает** с сетью
4. **Запустите контейнер** - он автоматически подключится к сети

### Оркестрация (опционально)

Для управления множеством валидаторов используйте:
- **Kubernetes**: Для больших сетей и автоматического масштабирования
- **Docker Swarm**: Для средних сетей
- **Standalone Docker**: Для небольших сетей (каждый валидатор на своем сервере)
- **Cloud Orchestration**: AWS ECS, Google Cloud Run, Azure Container Instances

**Важно:** Каждый валидатор остается независимым контейнером, даже при использовании оркестрации.

---

## 📝 Резюме

**Что упаковывается в Docker образ:**
- ✅ Бинарник `volnixd-standalone` (статический, ~30MB)
- ✅ Минимальный runtime (Alpine Linux)
- ✅ Утилиты: jq, curl, netcat для работы и отладки
- ✅ Скрипты управления: entrypoint.sh, healthcheck.sh, node-info.sh
- ✅ Автоматическая инициализация при первом запуске
- ✅ Конфигурация и данные через volumes

**Назначение:**
- ✅ **Один Docker образ = один валидатор**
- ✅ Для production: каждый валидатор в отдельном контейнере на отдельном сервере
- ✅ Для тестирования: используйте `testnet/docker-compose.test.yml`

**Взаимодействие в сети:**
- ✅ P2P через реальные IP адреса и порты
- ✅ RPC доступен с хоста через проброшенные порты
- ✅ Все данные персистентны через Docker volumes
- ✅ Каждый валидатор имеет свой изолированный volume

**Production-ready:**
- ✅ Resource limits (настраиваются при запуске)
- ✅ Health checks (встроенные)
- ✅ Logging с ротацией (настраивается)
- ✅ Security best practices (non-root user, read-only config)
- ✅ Автоматическая инициализация

**Документация:**
- 📖 [DOCKER_DEPLOYMENT.md](DOCKER_DEPLOYMENT.md) - Инструкции для пользователей
- 📖 `testnet/docker-compose.test.yml` - Для локального тестирования сети

