# 🔄 State Sync для быстрой синхронизации

## Обзор

State Sync позволяет новым узлам быстро синхронизироваться с сетью, загружая снимки состояния вместо полной синхронизации всех блоков с genesis. Это значительно ускоряет процесс синхронизации для новых узлов.

## Включение State Sync

### Через переменные окружения

```bash
# Включить State Sync
export VOLNIX_STATE_SYNC_ENABLE=true

# Указать RPC серверы для получения снимков (через запятую)
export VOLNIX_STATE_SYNC_RPC_SERVERS="http://node1:26657,http://node2:26657,http://node3:26657"

# Опционально: указать trust height и hash
export VOLNIX_STATE_SYNC_TRUST_HEIGHT=100000
export VOLNIX_STATE_SYNC_TRUST_HASH="ABCDEF123456..."

# Опционально: период доверия (по умолчанию 168h = 1 неделя)
export VOLNIX_STATE_SYNC_TRUST_PERIOD=168h

# Опционально: время обнаружения снимков (по умолчанию 15s)
export VOLNIX_STATE_SYNC_DISCOVERY_TIME=15s

# Запуск узла
./build/volnixd-standalone start
```

### Через config.toml

State Sync также можно настроить напрямую в `config.toml`:

```toml
[statesync]
enable = true
rpc_servers = "http://node1:26657,http://node2:26657,http://node3:26657"
trust_height = 100000
trust_hash = "ABCDEF123456..."
trust_period = "168h0m0s"
discovery_time = "15s"
```

## Как это работает

1. **Обнаружение снимков**: Узел запрашивает список доступных снимков у RPC серверов
2. **Выбор снимка**: Выбирается подходящий снимок (обычно самый свежий)
3. **Загрузка чанков**: Снимок загружается по частям (чанкам)
4. **Применение снимка**: Снимок применяется к состоянию узла
5. **Синхронизация**: Узел синхронизируется с текущей высотой блока

## Требования

### Для узлов, предоставляющих снимки

Узлы, которые будут предоставлять снимки для State Sync, должны:
- Иметь включенный RPC сервер
- Быть полностью синхронизированными
- Иметь достаточно ресурсов для обслуживания запросов

### Для узлов, использующих State Sync

- Доступ к RPC серверам с снимками
- Достаточно места на диске для снимка
- Стабильное интернет-соединение

## Trust Height и Trust Hash

**Trust Height** - высота блока, на которой был создан снимок. Узел доверяет этому блоку.

**Trust Hash** - хеш блока на trust height. Используется для проверки целостности.

**Trust Period** - период времени, в течение которого trust height/hash считаются валидными.

### Автоматическое обнаружение

Если `trust_height` и `trust_hash` не указаны, CometBFT автоматически обнаружит их из сети. Это рекомендуется для большинства случаев.

### Ручная настройка

Для большей безопасности можно вручную указать trust height и hash:

```bash
# Получить trust height и hash из сети
curl http://node1:26657/status | jq '.result.sync_info.latest_block_height, .result.sync_info.latest_block_hash'

# Установить в переменные окружения
export VOLNIX_STATE_SYNC_TRUST_HEIGHT=100000
export VOLNIX_STATE_SYNC_TRUST_HASH="ABCDEF123456..."
```

## Примеры использования

### Быстрый старт нового узла

```bash
# 1. Включить State Sync
export VOLNIX_STATE_SYNC_ENABLE=true
export VOLNIX_STATE_SYNC_RPC_SERVERS="http://mainnet-node1:26657,http://mainnet-node2:26657"

# 2. Запустить узел
./build/volnixd-standalone start
```

### Мультинод сеть с State Sync

```bash
# Для каждого нового узла
export VOLNIX_STATE_SYNC_ENABLE=true
export VOLNIX_STATE_SYNC_RPC_SERVERS="http://node0:26657,http://node1:26657,http://node2:26657"
export VOLNIX_HOME=".volnix"
export VOLNIX_RPC_PORT=26657
export VOLNIX_P2P_PORT=26656

./build/volnixd-standalone start
```

## Проверка статуса синхронизации

```bash
# Проверить статус синхронизации
curl http://localhost:26657/status | jq '.result.sync_info'

# Проверить, используется ли State Sync
curl http://localhost:26657/status | jq '.result.sync_info.catching_up'
```

## Troubleshooting

### State Sync не работает

1. **Проверьте, что State Sync включен:**
   ```bash
   grep "enable = true" ~/.volnix/config/config.toml | grep statesync
   ```

2. **Проверьте доступность RPC серверов:**
   ```bash
   curl http://node1:26657/status
   ```

3. **Проверьте логи узла:**
   ```bash
   tail -f ~/.volnix/logs/node.log | grep -i "state.*sync"
   ```

### Узел не может найти снимки

- Убедитесь, что RPC серверы доступны
- Проверьте, что RPC серверы полностью синхронизированы
- Увеличьте `discovery_time` если сеть медленная

### Снимок не применяется

- Проверьте, что trust height и hash корректны
- Убедитесь, что trust period не истек
- Проверьте логи на наличие ошибок

## Безопасность

### Рекомендации

1. **Используйте доверенные RPC серверы**: State Sync требует доверия к RPC серверам
2. **Проверяйте trust hash**: Всегда проверяйте trust hash из надежных источников
3. **Ограничьте trust period**: Не устанавливайте слишком длинный trust period
4. **Используйте несколько RPC серверов**: Для большей надежности используйте несколько источников

### Ограничения

- State Sync требует доверия к RPC серверам
- Не подходит для валидаторов (валидаторы должны синхронизироваться полностью)
- Требует стабильного интернет-соединения

## Сравнение с Block Sync

| Характеристика | State Sync | Block Sync |
|---------------|------------|------------|
| Скорость | Быстро (минуты) | Медленно (часы/дни) |
| Требует доверия | Да | Нет |
| Подходит для валидаторов | Нет | Да |
| Использование ресурсов | Низкое | Высокое |
| Требования к сети | Средние | Низкие |

## Дополнительные ресурсы

- [CometBFT State Sync Documentation](https://docs.cometbft.com/v0.38/core/state-sync)
- [Cosmos SDK State Sync Guide](https://docs.cosmos.network/main/core/state-sync)


