# Volnix Protocol - Контекст проекта

> Этот документ разделяет проект на логические части для удобного понимания архитектуры и быстрой навигации.

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Архитектурные слои](#архитектурные-слои)
3. [Компонент 1: Blockchain Core](#компонент-1-blockchain-core)
4. [Компонент 2: Custom Modules](#компонент-2-custom-modules)
5. [Компонент 3: Frontend Applications](#компонент-3-frontend-applications)
6. [Компонент 4: Infrastructure & Scripts](#компонент-4-infrastructure--scripts)
7. [Компонент 5: Protocol Definitions](#компонент-5-protocol-definitions)
8. [Компонент 6: Testing](#компонент-6-testing)
9. [Ключевые константы](#ключевые-константы)
10. [Взаимосвязи](#взаимосвязи)

---

## Обзор проекта

**Volnix Protocol** - блокчейн на базе Cosmos SDK v0.53.4 с консенсусом CometBFT v0.38.17.

### Технологический стек
- **Backend**: Go 1.23.2, Cosmos SDK v0.53.4, CometBFT v0.38.17
- **Frontend**: React + TypeScript, CosmJS
- **Storage**: GoLevelDB
- **Protocol**: Protobuf для определения сообщений

### Основные функции
- Управление идентичностью с ZKP (x/ident)
- Лицензирование майнинга (x/lizenz)
- Внутренний рынок токенов (x/anteil)
- Консенсус Proof-of-Verified-Burn (x/consensus)

---

## Архитектурные слои

```
┌─────────────────────────────────────────┐
│  Frontend Layer                         │
│  - Wallet UI (React)                    │
│  - Blockchain Explorer (HTML/JS)        │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│  API Layer                               │
│  - RPC (Port 26657)                     │
│  - gRPC (Port 9090)                     │
│  - REST (Port 1317)                     │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│  Application Layer                      │
│  - Volnix App (app/app.go)              │
│  - Module Manager                       │
│  - ABCI Handlers                        │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│  Custom Modules Layer                   │
│  - x/ident, x/lizenz, x/anteil,         │
│    x/consensus                          │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│  Consensus Layer                        │
│  - CometBFT Engine                      │
│  - PoVB Logic                           │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│  Storage Layer                          │
│  - State DB (GoLevelDB)                 │
│  - Block Store                          │
└─────────────────────────────────────────┘
```

---

## Компонент 1: Blockchain Core

**Назначение**: Ядро блокчейна - приложение, серверы, ABCI интерфейс.

### Структура директорий
```
app/                    # Основное приложение
├── app.go              # VolnixApp - главный класс приложения
├── server.go           # HTTP/gRPC сервер
├── minimal_app.go      # Минимальная версия для тестов
├── minimal_server.go   # Минимальный сервер
├── abci_wrapper.go     # ABCI обертка для CometBFT
├── ante.go             # AnteHandler для валидации транзакций
├── encoding.go         # Кодирование транзакций
├── config.go           # Конфигурация приложения
└── monitoring.go       # Метрики и мониторинг

cmd/                    # Исполняемые файлы
├── volnixd/            # Полная версия узла
│   ├── main.go         # Точка входа для полной сети
│   └── main_network.go # Сетевая конфигурация
└── volnixd-standalone/ # Standalone версия (для тестов)
    └── main.go         # Минимальный узел без модулей
```

### Ключевые файлы

#### `app/app.go` - Главное приложение
- **Класс**: `VolnixApp`
- **Назначение**: Координирует работу всех модулей
- **Ключевые методы**:
  - `NewVolnixApp()` - создание приложения
  - `BeginBlock()`, `EndBlock()` - обработка блоков
  - `InitChainer()` - инициализация цепочки
- **Зависимости**: Все модули из `x/`

#### `cmd/volnixd-standalone/main.go` - Standalone узел
- **Класс**: `StandaloneApp`, `StandaloneServer`
- **Назначение**: Минимальный узел для тестирования без модулей
- **Особенности**:
  - Использует только BaseApp (без кастомных модулей)
  - Chain ID: `volnix-standalone`
  - Обрабатывает банковские запросы через кастомный `Query()`
- **Использование**: Локальное тестирование, разработка фронтенда

#### `app/abci_wrapper.go` - ABCI интерфейс
- **Назначение**: Мост между Cosmos SDK и CometBFT
- **Методы**: `CheckTx`, `FinalizeBlock`, `Commit`, `Query`, `Info`, `InitChain`

### Важные константы
- **Chain ID (standalone)**: `volnix-standalone`
- **Chain ID (testnet)**: `volnix-testnet`
- **RPC Port**: `26657`
- **P2P Port**: `26656`
- **gRPC Port**: `9090`
- **REST Port**: `1317`

---

## Компонент 2: Custom Modules

**Назначение**: Бизнес-логика протокола - 4 кастомных модуля Cosmos SDK.

### Структура
```
x/
├── ident/          # Identity Module - управление идентичностью
├── lizenz/         # License Module - лицензии на майнинг
├── anteil/         # Market Module - внутренний рынок ANT
└── consensus/      # Consensus Module - логика PoVB
```

### Модуль 1: Identity (`x/ident`)

**Назначение**: Управление ролями пользователей с ZKP верификацией.

**Роли**:
- `GUEST` - гость (базовая роль)
- `CITIZEN` - гражданин (верифицированная личность)
- `VALIDATOR` - валидатор (может создавать блоки)

**Ключевые файлы**:
- `keeper/keeper.go` - бизнес-логика модуля
- `types/` - типы данных (VerifiedAccount, Role)
- `client/cli/` - CLI команды
- `module.go` - регистрация модуля

**Proto файлы**: `proto/volnix/ident/v1/`

**Основные операции**:
- `VerifyIdentity()` - верификация личности с ZKP
- `UpdateRole()` - изменение роли
- `GetRole()` - получение роли аккаунта

---

### Модуль 2: Lizenz (`x/lizenz`)

**Назначение**: Управление лицензиями на майнинг (LZN токены).

**Ключевые концепции**:
- **LZN (Lizenz Token)**: Токен лицензии на майнинг
- **MOA (Minimum Operating Activity)**: Минимальная активность для валидатора
- **Activation**: Активация лицензии для майнинга

**Ключевые файлы**:
- `keeper/keeper.go` - логика лицензий
- `types/` - Lizenz, Activation
- `client/cli/` - CLI команды

**Proto файлы**: `proto/volnix/lizenz/v1/`

**Основные операции**:
- `ActivateLizenz()` - активация лицензии
- `CheckMOA()` - проверка минимальной активности
- `DeactivateLizenz()` - деактивация

---

### Модуль 3: Anteil (`x/anteil`)

**Назначение**: Внутренний рынок токенов ANT (Anteil).

**Ключевые концепции**:
- **ANT (Anteil Token)**: Внутренний токен протокола
- **Order**: Ордер на покупку/продажу ANT
- **Auction**: Аукционная система для ANT

**Ключевые файлы**:
- `keeper/keeper.go` - логика рынка
- `types/` - Order, Auction
- `client/cli/` - CLI команды

**Proto файлы**: `proto/volnix/anteil/v1/`

**Основные операции**:
- `PlaceOrder()` - размещение ордера
- `MatchOrders()` - сопоставление ордеров
- `StartAuction()` - запуск аукциона

---

### Модуль 4: Consensus (`x/consensus`)

**Назначение**: Реализация консенсуса Proof-of-Verified-Burn (PoVB).

**Ключевые концепции**:
- **PoVB**: Консенсус на основе сжигания токенов
- **Validator Selection**: Выбор валидаторов на основе сжигания
- **Burn Transaction**: Транзакция сжигания токенов

**Ключевые файлы**:
- `keeper/keeper.go` - логика консенсуса
- `types/` - Validator, BurnRecord
- `client/cli/` - CLI команды

**Proto файлы**: `proto/volnix/consensus/v1/`

**Основные операции**:
- `ExecuteBurn()` - выполнение сжигания
- `SelectValidators()` - выбор валидаторов
- `UpdateValidatorSet()` - обновление набора валидаторов

---

## Компонент 3: Frontend Applications

**Назначение**: Пользовательские интерфейсы для взаимодействия с блокчейном.

### Структура
```
frontend/
├── wallet-ui/              # React приложение - кошелек
│   ├── src/
│   │   ├── App.tsx         # Главный компонент
│   │   ├── components/      # UI компоненты
│   │   │   ├── WalletConnect.tsx    # Подключение кошелька
│   │   │   ├── Balance.tsx           # Отображение балансов
│   │   │   ├── SendTokens.tsx       # Отправка токенов
│   │   │   ├── TransactionHistory.tsx # История транзакций
│   │   │   ├── RoleSelection.tsx     # Выбор роли
│   │   │   ├── CitizenDashboard.tsx  # Дашборд гражданина
│   │   │   ├── ValidatorDashboard.tsx # Дашборд валидатора
│   │   │   ├── AntMarket.tsx         # Рынок ANT
│   │   │   └── ValidatorManagement.tsx # Управление валидаторами
│   │   ├── services/
│   │   │   └── blockchainService.ts  # Сервис для работы с блокчейном
│   │   └── types/
│   │       └── wallet.ts              # Типы для кошелька
│   └── package.json
└── blockchain-explorer/    # HTML/JS приложение - эксплорер
    ├── index.html          # Главная страница
    └── README.md
```

### Wallet UI - Ключевые компоненты

#### `src/services/blockchainService.ts`
- **Класс**: `BlockchainService`
- **Назначение**: Взаимодействие с RPC узлом через CosmJS
- **Ключевые методы**:
  - `initializeClient()` - подключение StargateClient
  - `initializeSigningClient(mnemonic)` - подключение SigningStargateClient
  - `getBalances(address)` - получение балансов
  - `sendTokens()` - отправка токенов
- **Конфигурация**:
  - `RPC_ENDPOINT`: `http://localhost:26657`
  - `CHAIN_ID`: `volnix-standalone`
  - `PREFIX`: `volnix`

#### `src/components/WalletConnect.tsx`
- **Назначение**: Подключение кошелька (создание/импорт)
- **Функции**:
  - Создание нового кошелька
  - Подключение существующего (по мнемонике)
  - Демо-кошелек

#### `src/components/Balance.tsx`
- **Назначение**: Отображение балансов WRT, LZN, ANT
- **Источник данных**: `blockchainService.getBalances()`

### Blockchain Explorer
- **Технологии**: HTML, JavaScript, PowerShell
- **Назначение**: Просмотр блоков, транзакций, адресов
- **Запуск**: `start-explorer.ps1`

---

## Компонент 4: Infrastructure & Scripts

**Назначение**: Скрипты для запуска, тестирования и развертывания.

### Структура
```
scripts/
├── start-local-network.sh    # ⭐ Главный скрипт запуска локальной сети
├── start-testnet.sh          # Запуск тестнета
├── start-full-network.ps1    # Запуск полной сети (PowerShell)
├── deploy.sh                 # Развертывание
└── testing/
    └── volnix_tests.go       # Go тесты

testnet/                      # Конфигурация тестнета
├── genesis.json              # Генезис блок
└── node0/                    # Узел 0
    └── .volnix/              # Данные узла
        ├── config/
        │   ├── genesis.json
        │   └── config.toml
        └── data/             # База данных
```

### Ключевой скрипт: `start-local-network.sh`

**Назначение**: Запуск локальной сети для разработки.

**Что делает**:
1. Проверяет зависимости (Go, Node.js)
2. Собирает бинарники (`volnixd-standalone`)
3. Инициализирует узел в `testnet/node0`
4. Настраивает chain-id: `volnix-standalone`
5. Очищает базу данных перед запуском
6. Запускает RPC узел на порту 26657
7. Запускает Wallet UI на порту 3000
8. Запускает Blockchain Explorer на порту 8080

**Важные параметры**:
- `RPC_PORT=26657`
- `P2P_PORT=26656`
- `CHAIN_ID=volnix-standalone`
- `HOME_DIR=testnet/node0/.volnix`

**Логи**: `logs/node0.log`, `logs/wallet-ui.log`

---

## Компонент 5: Protocol Definitions

**Назначение**: Protobuf определения для всех сообщений и типов.

### Структура
```
proto/
├── volnix/
│   ├── ident/v1/         # Identity модуль
│   │   ├── tx.proto      # Транзакции
│   │   ├── query.proto   # Запросы
│   │   └── types.proto   # Типы данных
│   ├── lizenz/v1/        # Lizenz модуль
│   ├── anteil/v1/        # Anteil модуль
│   └── consensus/v1/     # Consensus модуль
├── buf.yaml              # Конфигурация buf
└── buf.gen.yaml          # Генерация кода

gen/go/                    # Сгенерированный Go код
└── volnix/...
```

### Процесс генерации
1. Определения в `.proto` файлах
2. Генерация через `buf generate`
3. Go код в `proto/gen/go/`
4. Импорт в модулях через `github.com/volnix-protocol/volnix-protocol/proto/gen/go/volnix/...`

---

## Компонент 6: Testing

**Назначение**: Тесты для всех компонентов.

### Структура
```
tests/
├── simple_test.go         # Простые unit тесты
├── integration_test.go    # Интеграционные тесты
├── end_to_end_test.go     # E2E тесты
├── benchmark_test.go      # Бенчмарки
└── security_test.go       # Тесты безопасности
```

### Типы тестов
- **Unit тесты**: Отдельные функции/методы
- **Integration тесты**: Взаимодействие модулей
- **E2E тесты**: Полные сценарии пользователей
- **Benchmark тесты**: Производительность

---

## Ключевые константы

### Chain IDs
```go
const (
    ChainIDStandalone = "volnix-standalone"  // Standalone узел
    ChainIDTestnet    = "volnix-testnet"     // Тестнет
    ChainIDMainnet    = "volnix-mainnet"     // Мейннет (будущее)
)
```

### Порты
```go
const (
    RPCPort    = 26657  // RPC сервер
    P2PPort    = 26656  // P2P сеть
    GRPCPort   = 9090   // gRPC сервер
    RESTPort   = 1317   // REST API
    WalletPort = 3000   // Wallet UI
    ExplorerPort = 8080 // Blockchain Explorer
)
```

### Деноминации токенов
```go
const (
    DenomWRT = "uwrt"   // Base token (micro WRT)
    DenomLZN = "ulzn"   // License token (micro LZN)
    DenomANT = "uant"   // Anteil token (micro ANT)
)
```

### Префиксы адресов
```go
const (
    AddressPrefix = "volnix"  // Bech32 префикс для адресов
)
```

---

## Взаимосвязи

### Поток транзакции
```
Frontend (Wallet UI)
    ↓ (CosmJS)
RPC Endpoint (Port 26657)
    ↓ (ABCI)
VolnixApp (app/app.go)
    ↓ (Router)
Custom Module (x/ident, x/lizenz, etc.)
    ↓ (Keeper)
State Store (GoLevelDB)
```

### Поток запроса
```
Frontend (getBalances)
    ↓ (CosmJS StargateClient)
RPC Endpoint (/cosmos.bank.v1beta1.Query/AllBalances)
    ↓ (ABCI Query)
VolnixApp.Query()
    ↓ (Custom Query Handler)
Response (JSON)
    ↓
Frontend (отображение балансов)
```

### Зависимости модулей
```
VolnixApp
    ├── x/ident (независимый)
    ├── x/lizenz (может использовать x/ident)
    ├── x/anteil (может использовать x/ident, x/lizenz)
    └── x/consensus (использует все модули для PoVB)
```

---

## Быстрая навигация

### Если нужно изменить:
- **Транзакции**: `proto/volnix/*/v1/tx.proto` → `x/*/keeper/`
- **Запросы**: `proto/volnix/*/v1/query.proto` → `x/*/keeper/`
- **UI компонент**: `frontend/wallet-ui/src/components/`
- **Подключение к блокчейну**: `frontend/wallet-ui/src/services/blockchainService.ts`
- **Запуск сети**: `scripts/start-local-network.sh`
- **Конфигурация узла**: `testnet/node0/.volnix/config/`
- **Главное приложение**: `app/app.go`
- **Standalone узел**: `cmd/volnixd-standalone/main.go`

### Если нужно понять:
- **Архитектуру**: `docs/core-architecture.md`
- **Модули**: `docs/module-architecture.md`
- **Экономику**: `docs/economic-model.md`
- **Консенсус**: `docs/povb-consensus-mechanism.md`

---

## Важные замечания

### Standalone узел
- Используется только для разработки фронтенда
- Не содержит кастомных модулей (только BaseApp)
- Chain ID жестко закодирован: `volnix-standalone`
- Обрабатывает банковские запросы через кастомный `Query()`

### Полная сеть
- Использует все модули из `x/`
- Chain ID настраивается в genesis.json
- Требует полной инициализации модулей

### Frontend
- Использует CosmJS для подключения
- Поддерживает только standalone узел (для разработки)
- Для полной сети нужны дополнительные query handlers

---

**Последнее обновление**: 2025-01-27
**Версия документа**: 1.0

