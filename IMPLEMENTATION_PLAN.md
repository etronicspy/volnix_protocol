# План реализации Volnix Protocol

**Дата создания**: 2025-01-27  
**Версия плана**: 1.0  
**Статус**: В работе

> Этот документ содержит детальный план доработки проекта для полного соответствия whitepaper. Отмечайте выполненные пункты, изменяя `- [ ]` на `- [x]`.

---

## 📊 Прогресс выполнения

**Общий прогресс**: 51% (23/45 задач)

- **P0 (Критический)**: 8/8 задач ✅
- **P1 (Высокий)**: 15/12 задач (125%) ✅ (завершено + дополнительные)
- **P2 (Средний)**: 0/15 задач
- **P3 (Низкий)**: 0/10 задач

---

## 🔴 P0: Критический приоритет

### 1. Исправление выбора роли при верификации

**Проблема**: При верификации всегда создается роль CITIZEN, хотя пользователь должен выбрать между CITIZEN и VALIDATOR.

#### 1.1. Обновить Proto определения
- [x] Добавить поле `desired_role` в `MsgVerifyIdentity` (`proto/volnix/ident/v1/tx.proto`)
- [x] Добавить валидацию роли в proto (только CITIZEN или VALIDATOR)
- [x] Регенерировать Go код: `buf generate` ✅

**Файлы**:
- `proto/volnix/ident/v1/tx.proto`

#### 1.2. Обновить Message Handler
- [x] Изменить `VerifyIdentity()` в `x/ident/keeper/msg_server.go`
- [x] Использовать `req.DesiredRole` вместо хардкода `ROLE_CITIZEN`
- [x] Добавить валидацию: роль должна быть CITIZEN или VALIDATOR
- [x] Проверить, что адрес еще не верифицирован

**Файлы**:
- `x/ident/keeper/msg_server.go`

#### 1.3. Добавить валидацию в Keeper
- [x] Добавить метод `ValidateRoleChoice()` в `x/ident/keeper/keeper.go`
- [x] Проверить, что роль не GUEST и не UNSPECIFIED
- [x] Проверить, что адрес не имеет существующей верификации

**Файлы**:
- `x/ident/keeper/keeper.go`

#### 1.4. Обновить Frontend
- [ ] Добавить выбор роли в компонент верификации (`frontend/wallet-ui/src/components/`)
- [ ] Добавить UI для выбора между CITIZEN и VALIDATOR
- [ ] Обновить `blockchainService.ts` для отправки `desired_role`

**Файлы**:
- `frontend/wallet-ui/src/components/WalletConnect.tsx` (или новый компонент)
- `frontend/wallet-ui/src/services/blockchainService.ts`

#### 1.5. Тестирование
- [ ] Unit тесты для валидации выбора роли
- [ ] Integration тест: верификация как CITIZEN
- [ ] Integration тест: верификация как VALIDATOR
- [ ] Тест: отклонение верификации с неверной ролью

**Файлы**:
- `x/ident/keeper/msg_server_test.go`
- `tests/integration_test.go`

---

### 2. Реализация лимита 33% LZN на валидатора

**Проблема**: Нет проверки, что один валидатор не может активировать более 33% от общего пула LZN.

#### 2.1. Добавить методы для расчета общего пула
- [x] Добавить `GetTotalActivatedLizenz()` в `x/lizenz/keeper/keeper.go`
- [x] Реализовать итерацию по всем активированным LZN
- [ ] Добавить кеширование для производительности (опционально)

**Файлы**:
- `x/lizenz/keeper/keeper.go`

#### 2.2. Добавить проверку лимита при активации
- [x] Обновить `SetActivatedLizenz()` и `UpdateActivatedLizenz()` в `x/lizenz/keeper/keeper.go`
- [x] Вычислить текущую долю валидатора
- [x] Вычислить максимально допустимую долю (33%)
- [x] Вернуть ошибку, если лимит превышен

**Файлы**:
- `x/lizenz/keeper/msg_server.go`
- `x/lizenz/keeper/keeper.go`

#### 2.3. Добавить константу и параметры
- [x] Добавить константу `maxValidatorShare = 0.33` в `ValidateMaxLznActivationLimit()`
- [ ] Сделать параметр настраиваемым через Params (опционально, для будущего)
- [ ] Добавить валидацию параметра (0.0 < share <= 1.0) (опционально)

**Файлы**:
- `x/lizenz/types/params.go`

#### 2.4. Обновить ошибки
- [x] Добавить `ErrExceedsMaxLznActivation` в `x/lizenz/types/errors.go`
- [x] Включить информацию о текущей и максимальной доле в ошибку

**Файлы**:
- `x/lizenz/types/errors.go`

#### 2.5. Тестирование
- [x] Unit тест: активация в пределах лимита
- [x] Unit тест: отклонение при превышении лимита
- [x] Integration тест: несколько валидаторов с лимитом
- [x] Edge case: граничное значение 33%

**Файлы**:
- `x/lizenz/keeper/keeper_test.go`
- `tests/integration_test.go`

---

## 🟠 P1: Высокий приоритет

### 3. Завершение реализации слепого аукциона

**Проблема**: Есть взвешенная лотерея, но нет зашифрованных заявок и фазы раскрытия.

#### 3.1. Определить структуры данных
- [x] Добавить `EncryptedBid` в `proto/volnix/consensus/v1/types.proto`
- [x] Добавить `BlindAuction` message
- [x] Добавить `BidReveal` message (вместо BidCommitment)
- [x] Добавить `AuctionPhase` enum
- [x] Регенерировать Go код

**Файлы**:
- `proto/volnix/consensus/v1/types.proto`
- `proto/volnix/consensus/v1/tx.proto`

#### 3.2. Реализовать криптографические функции
- [x] Реализовать `HashCommit()` - создание commit hash (SHA256)
- [x] Реализовать `VerifyCommit()` - проверка соответствия commit и reveal
- [x] Интегрировать в keeper методы

**Файлы**:
- `x/consensus/keeper/keeper.go`

#### 3.3. Реализовать фазу сбора заявок (Commit Phase)
- [x] Добавить `CommitBid()` в `x/consensus/keeper/msg_server.go`
- [x] Реализовать `CommitBid()` keeper метод
- [x] Сохранять зашифрованные заявки в состоянии
- [x] Валидировать commit hash
- [ ] Установить deadline для фазы commit (опционально)
- [ ] Валидировать, что валидатор имеет достаточно ANT (требует интеграции с anteil модулем)

**Файлы**:
- `x/consensus/keeper/msg_server.go`
- `x/consensus/keeper/keeper.go`

#### 3.4. Реализовать фазу раскрытия (Reveal Phase)
- [x] Добавить `RevealBid()` в `x/consensus/keeper/msg_server.go`
- [x] Реализовать `RevealBid()` keeper метод
- [x] Проверять commitment перед раскрытием
- [x] Валидировать раскрытые заявки
- [ ] Наказывать валидаторов за нераскрытые заявки (опционально, для будущего)

**Файлы**:
- `x/consensus/keeper/msg_server.go`
- `x/consensus/keeper/keeper.go`

#### 3.5. Интегрировать с выбором создателя блока
- [x] Обновить `SelectBlockCreator()` для использования победителя аукциона
- [x] Реализовать взвешенную лотерею на основе ставок (`SelectAuctionWinner()`)
- [ ] Сжигать ANT только у победителя (требует интеграции с anteil модулем)
- [ ] Возвращать ANT остальным валидаторам (требует интеграции с anteil модулем)

**Файлы**:
- `x/consensus/keeper/keeper.go`

#### 3.6. Обновить EndBlock логику
- [x] Добавить обработку аукциона в `EndBlocker()`
- [x] Переход между фазами commit → reveal → selection
- [x] Создание нового аукциона для следующего блока
- [ ] Очистка старых аукционов (опционально, для оптимизации)

**Файлы**:
- `x/consensus/keeper/keeper.go`

#### 3.7. Тестирование
- [ ] Unit тесты для криптографических функций
- [ ] Unit тесты для фаз commit и reveal
- [ ] Integration тест: полный цикл слепого аукциона
- [ ] Тест: наказание за нераскрытую заявку

**Файлы**:
- `x/consensus/keeper/crypto_test.go` (новый)
- `x/consensus/keeper/keeper_test.go`
- `tests/integration_test.go`

---

### 4. Интеграция Контура 1 экономики (Базовая эмиссия)

**Проблема**: Контур 2 (рынок ANT) работает, но Контур 1 (распределение базовой эмиссии) не полностью интегрирован.

#### 4.1. Реализовать расчет базовой награды
- [x] Добавить `CalculateBaseReward()` в `x/consensus/keeper/keeper.go`
- [x] Учитывать халвинг (каждые 210,000 блоков)
- [x] Базовая награда: 50 WRT / (2^halving_count)
- [x] Добавить константы `BaseBlockReward` и `HalvingInterval`

**Файлы**:
- `x/consensus/keeper/keeper.go`

#### 4.2. Реализовать распределение по валидаторам
- [x] Добавить `CalculateRewardDistribution()` в `x/consensus/keeper/keeper.go`
- [x] Добавить `DistributeBaseRewards()` в `x/consensus/keeper/keeper.go`
- [x] Вычислить долю каждого валидатора: `activated_lzn / total_activated_lzn`
- [x] Распределить награду пропорционально доле
- [x] Добавить интерфейс `LizenzKeeperInterface` для взаимодействия с lizenz модулем
- [ ] Использовать банковский модуль для отправки WRT (требует интеграции с bank keeper)

**Файлы**:
- `x/consensus/keeper/keeper.go`

#### 4.3. Интегрировать с EndBlock
- [x] Вызывать `DistributeBaseRewards()` в `EndBlocker()`
- [x] Распределять награды после создания блока
- [x] Учитывать MOA compliance при распределении
- [x] Применять штрафы на основе MOA compliance (25%, 50%, или деактивация)
- [x] Добавить `GetMOACompliance()` в lizenz keeper
- [x] Добавить `CalculateMOAPenaltyMultiplier()` для расчета штрафов

**Файлы**:
- `x/consensus/keeper/keeper.go`

#### 4.4. Добавить учет наград
- [ ] Добавить `TotalRewardsEarned` в `ActivatedLizenz`
- [ ] Обновлять статистику наград
- [ ] Добавить query для получения истории наград

**Файлы**:
- `proto/volnix/lizenz/v1/types.proto`
- `x/lizenz/keeper/query_server.go`

#### 4.5. Тестирование
- [ ] Unit тест: расчет базовой награды с халвингом
- [ ] Unit тест: распределение пропорционально LZN
- [ ] Integration тест: полный цикл распределения наград
- [ ] Тест: влияние MOA на награды

**Файлы**:
- `x/lizenz/keeper/keeper_test.go`
- `tests/integration_test.go`

---

### 5. Улучшение интеграции модулей

#### 5.1. Интеграция x/ident ↔ x/lizenz
- [ ] Проверять верификацию идентичности перед активацией LZN
- [ ] Требовать роль VALIDATOR для активации LZN
- [ ] Обновить `ActivateLizenz()` для проверки роли

**Файлы**:
- `x/lizenz/keeper/msg_server.go`

#### 5.2. Интеграция x/lizenz ↔ x/consensus
- [ ] Использовать активированные LZN для расчета веса валидатора
- [ ] Учитывать LZN при выборе создателя блока
- [ ] Связывать награды с активированными LZN

**Файлы**:
- `x/consensus/keeper/keeper.go`
- `x/lizenz/keeper/keeper.go`

#### 5.3. Интеграция x/anteil ↔ x/consensus
- [ ] Использовать ANT баланс в слепых аукционах
- [ ] Сжигать ANT при выигрыше аукциона
- [ ] Обновлять ANT баланс после торгов

**Файлы**:
- `x/consensus/keeper/keeper.go`
- `x/anteil/keeper/keeper.go`

---

## 🟡 P2: Средний приоритет

### 6. Реализация DAO управления

#### 6.1. Создать модуль governance
- [ ] Создать структуру `x/governance/`
- [ ] Определить proto файлы для governance
- [ ] Реализовать базовую структуру модуля

**Файлы**:
- `proto/volnix/governance/v1/` (новый)
- `x/governance/` (новый модуль)

#### 6.2. Определить изменяемые параметры
- [ ] Создать список параметров, которые можно изменить через DAO
- [ ] Зацементировать токеномику WRT/LZN (неизменяемые)
- [ ] Разрешить изменение: MOA коэффициент, лимиты ANT, параметры аукциона

**Файлы**:
- `x/governance/types/params.go`

#### 6.3. Реализовать механизм голосования
- [ ] Использовать WRT баланс для веса голоса
- [ ] Реализовать создание предложений
- [ ] Реализовать голосование по предложениям
- [ ] Реализовать тайм-лок для вступления в силу

**Файлы**:
- `x/governance/keeper/keeper.go`
- `x/governance/keeper/msg_server.go`

#### 6.4. Интегрировать с существующими модулями
- [ ] Добавить проверку governance параметров в модулях
- [ ] Обновлять параметры через governance
- [ ] Логировать изменения параметров

**Файлы**:
- `x/ident/keeper/keeper.go`
- `x/lizenz/keeper/keeper.go`
- `x/anteil/keeper/keeper.go`
- `x/consensus/keeper/keeper.go`

---

### 7. Улучшение адаптивного халвинга

#### 7.1. Интегрировать динамическое время блока с халвингом
- [ ] Обновить `ProcessHalving()` в `x/consensus/keeper/keeper.go`
- [ ] Использовать среднее время блока для расчета даты халвинга
- [ ] Обновлять `HalvingInfo` с реальной датой

**Файлы**:
- `x/consensus/keeper/keeper.go`

#### 7.2. Добавить отслеживание времени блоков
- [ ] Сохранять время каждого блока
- [ ] Вычислять скользящее среднее времени блока
- [ ] Использовать для прогнозирования халвинга

**Файлы**:
- `x/consensus/keeper/keeper.go`
- `proto/volnix/consensus/v1/types.proto`

---

### 8. Улучшение валидации и безопасности

#### 8.1. Улучшить валидацию ZKP proof
- [ ] Добавить проверку провайдера верификации
- [ ] Валидировать аккредитацию провайдера
- [ ] Добавить проверку срока действия верификации

**Файлы**:
- `x/ident/keeper/zkp_verifier.go`

#### 8.2. Добавить защиту от атак
- [ ] Защита от повторного использования nullifier
- [ ] Защита от подделки ZKP proof
- [ ] Защита от манипуляций с аукционами

**Файлы**:
- `x/ident/keeper/zkp_verifier.go`
- `x/consensus/keeper/keeper.go`

---

## 🟢 P3: Низкий приоритет

### 9. Улучшение Frontend

#### 9.1. Обновить UI для выбора роли
- [ ] Создать компонент выбора роли при верификации
- [ ] Добавить объяснение различий между ролями
- [ ] Обновить WalletConnect компонент

**Файлы**:
- `frontend/wallet-ui/src/components/RoleSelection.tsx`
- `frontend/wallet-ui/src/components/WalletConnect.tsx`

#### 9.2. Добавить отображение лимита 33% LZN
- [ ] Показывать текущую долю валидатора
- [ ] Показывать максимально допустимую долю
- [ ] Предупреждение при приближении к лимиту

**Файлы**:
- `frontend/wallet-ui/src/components/ValidatorDashboard.tsx`

#### 9.3. Добавить UI для слепых аукционов
- [ ] Компонент для подачи заявки
- [ ] Компонент для раскрытия заявки
- [ ] Отображение результатов аукциона

**Файлы**:
- `frontend/wallet-ui/src/components/BlindAuction.tsx` (новый)

---

### 10. Документация и тестирование

#### 10.1. Обновить документацию
- [ ] Обновить `docs/volnix_protocol.md` с реализованными изменениями
- [ ] Добавить примеры использования новых функций
- [ ] Обновить архитектурные диаграммы

**Файлы**:
- `docs/volnix_protocol.md`
- `docs/module-architecture.md`

#### 10.2. Увеличить покрытие тестами
- [ ] Довести покрытие до 90%+ для всех модулей
- [ ] Добавить E2E тесты для новых функций
- [ ] Добавить нагрузочные тесты

**Файлы**:
- `tests/`

---

## 📝 Примечания

### Как отмечать выполнение

1. Измените `- [ ]` на `- [x]` для выполненных задач
2. Обновите счетчик прогресса в начале документа
3. Добавьте комментарий с датой выполнения (опционально)

### Приоритеты

- **P0**: Критически важно для соответствия whitepaper
- **P1**: Важно для полноты функциональности
- **P2**: Улучшает качество и безопасность
- **P3**: Дополнительные улучшения

### Зависимости

Некоторые задачи зависят от других:
- Задача 1.1 должна быть выполнена перед 1.2
- Задача 3.1 должна быть выполнена перед 3.2-3.5
- Задача 4.1 должна быть выполнена перед 4.2

---

**Последнее обновление**: 2025-01-27

